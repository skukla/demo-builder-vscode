{
    "_what": "ADR-017 enforcement ledger (WEBVIEW ONLY). Same contract as ADR-015's: an entry is a KNOWN pre-ruling violation with its reason; the test fails on an unlisted violation, on a listed entry that no longer violates, and on an entry without a reason. Moved here 2026-08-29 (PL-17) from the ADR-015 ledger, where a pure React rule had been enforced under a document that mentions React zero times. `bundleStylesheets` rows are ADR-017 \u00a76 violations found by webview-stylesheet-bundles.test.ts. `dynamicClassSiteCeiling` is a RATCHET, not an exemption: it counts className expressions this check cannot read statically (template literals), so it measures how much of the surface is NOT covered. It may only go down.",
    "hookRefs": {
        "src/core/ui/hooks/useActivateOnKey.ts": "flagged by the coarse inline-literal detector \u2014 adjudicate: legitimate default value vs unstable reference",
        "src/core/ui/hooks/useFocusOnMount.ts": "flagged by the coarse inline-literal detector \u2014 adjudicate: legitimate default value vs unstable reference",
        "src/features/components/ui/hooks/useStoreDiscovery.ts": "flagged by the coarse inline-literal detector \u2014 adjudicate: legitimate default value vs unstable reference",
        "src/features/dashboard/ui/hooks/useDashboardStatus.ts": "flagged by the coarse inline-literal detector \u2014 adjudicate: legitimate default value vs unstable reference",
        "src/features/project-creation/ui/components/ConnectStoreStepContent.tsx": "flagged by the coarse inline-literal detector \u2014 adjudicate: legitimate default value vs unstable reference"
    },
    "bundleStylesheets": {},
    "dynamicClassSiteCeiling": 95,
    "_bundleStylesheets_note": "EMPTY, and it started at 8 rows across 3 classes on this check's first run (2026-08-29). All three were fixed in the same commit rather than filed: .text-orange-500 and .text-orange-600 moved from EDS's feature sheet to custom-spectrum.css (kept UNLAYERED, because eds-steps.css declares no @layer and putting them in one would have lowered their cascade priority), and .number-badge moved from wizard.css to custom-spectrum.css inside @layer theme (its original layer, so its cascade position did not move). Keeping the empty key rather than deleting it: the check still runs, so a new violation fails immediately instead of starting a fresh pile.",
    "_dynamicClassSiteCeiling_note": "151 -> 123 on 2026-08-29 by converting 10 template-literal className expressions to cn(), which this check can read. IMPORTANT: this number counts SITES PER BUNDLE, not distinct files \u2014 a shared component in five bundles contributes five. The distinct-site count is far smaller (36 before the conversion, of which 23 were `className={someProp}` pass-throughs whose real class is checked at the CALL site, so never a blind spot). What remains is one genuinely dynamic case (InlineNotice builds `inline-notice--${tone}`) and two helper-call sites, multiplied across bundles. 123 -> 105 on 2026-08-31 by PL-31: with the UI barrels gone, a bundle imports only the components it uses instead of everything the barrel re-exported, so the same dynamic-className sites are multiplied across fewer bundles. The count is sites PER BUNDLE, which is why deleting no component at all moved it. Then 105 -> 95 as the remaining UI barrels went (same cause).",
    "classesDefinedNowhere": {
        "complete": "used in src/features/project-creation/ui/components/BrandGallery.tsx \u2014 no rule defines it in that bundle",
        "config-section-footer": "used in src/core/ui/components/forms/ConfigSection.tsx \u2014 no rule defines it in that bundle",
        "control-panel": "used in src/core/ui/components/layout/ControlPanelLayout.tsx \u2014 no rule defines it in that bundle",
        "control-panel-body": "used in src/core/ui/components/layout/ControlPanelLayout.tsx \u2014 no rule defines it in that bundle",
        "control-panel-secondary-inner": "used in src/core/ui/components/layout/ControlPanelLayout.tsx \u2014 no rule defines it in that bundle",
        "dashboard-control-panel": "used in src/features/dashboard/ui/ProjectDashboardScreen.tsx \u2014 no rule defines it in that bundle",
        "dashboard-zone-label": "used in src/features/sidebar/ui/components/AiZone.tsx (+1 more) \u2014 no rule defines it in that bundle",
        "datapack-danger-detail": "used in src/features/data-installer/ui/components/ImportDatapackModal.tsx \u2014 no rule defines it in that bundle",
        "datapack-danger-value": "used in src/features/data-installer/ui/components/ImportDatapackModal.tsx \u2014 no rule defines it in that bundle",
        "done": "used in src/features/project-creation/ui/components/BuildYourProjectSummary.tsx \u2014 no rule defines it in that bundle",
        "field-help-button": "used in src/core/ui/components/forms/FieldHelpButton.tsx \u2014 no rule defines it in that bundle",
        "field-help-dialog": "used in src/core/ui/components/forms/FieldHelpButton.tsx \u2014 no rule defines it in that bundle",
        "inline-rename--editing": "used in src/core/ui/components/forms/InlineRenameField.tsx \u2014 no rule defines it in that bundle",
        "intflow-api-reason": "used in src/core/ui/components/selection/ApiAccessPicker.tsx \u2014 no rule defines it in that bundle",
        "page-header": "used in src/core/ui/components/layout/PageHeader.tsx \u2014 no rule defines it in that bundle",
        "page-header-inner": "used in src/core/ui/components/layout/PageHeader.tsx \u2014 no rule defines it in that bundle",
        "project-card-menu-button": "used in src/features/projects-dashboard/ui/components/ProjectCard.tsx \u2014 no rule defines it in that bundle",
        "project-row-menu-button": "used in src/features/projects-dashboard/ui/components/ProjectRow.tsx \u2014 no rule defines it in that bundle"
    },
    "_classesDefinedNowhere_note": "Classes a component asks for that NO stylesheet in that bundle defines \u2014 and that no stylesheet anywhere in src/ defines either. A different defect from bundleStylesheets, with a different fix: for these, either the rule was never written or the markup is dead. Seeded at 21 on 2026-08-29 so the set cannot GROW while it is triaged; the triage itself is PL-20. Two are visible bugs already (.text-red-500 on an error icon, .text-orange-700 on warning text) \u2014 they render with no colour. Note .text-red-500 IS defined, but inside VerifiedField's inline <style> block, so it exists only where that component is mounted; the scanner counts style-block classes as defined within their own bundle, which is why this reports only the bundles VerifiedField does not reach.",
    "messageChannelOwners": {
        "src/core/ui/utils/WebviewClient.ts": "THE singleton channel \u2014 the shared client every bundle except the sidebar talks through.",
        "src/features/sidebar/ui/index.tsx": "The sidebar is its own bundle, so its own webview, so its own single call. It does NOT use WebviewClient, and that was checked rather than assumed (2026-08-30): the two are variants, not duplication. The sidebar sends 15 one-way commands with two lines \u2014 postMessage plus a message listener \u2014 while WebviewClient is 311 lines of handshake, message queue, and request/response with ids and timeouts. Routing the sidebar through it would add a handshake it does not take part in. Not a candidate for consolidation."
    },
    "importantCeiling": 1969,
    "_importantCeiling_note": "ADR-018 measured 1,866 removable uses when it was written; the count had drifted to 1,969 by 2026-08-30, which is why it is pinned rather than left as advice. Migration is PL-21 and is not authorised yet \u2014 this only stops the number growing back.",
    "styleBlockLeaks": {
        "src/core/ui/components/wizard/ConfigurationSummary.tsx::font-medium": "Redundant copy \u2014 .font-medium is also in custom-spectrum.css, which all eight bundle entries import, so this block shadows a global definition rather than supplying a missing one. Measured 2026-08-30.",
        "src/core/ui/components/wizard/ConfigurationSummary.tsx::font-semibold": "Redundant copy \u2014 .font-semibold is also in custom-spectrum.css, which all eight bundle entries import, so this block shadows a global definition rather than supplying a missing one. Measured 2026-08-30.",
        "src/core/ui/components/wizard/ConfigurationSummary.tsx::letter-spacing-05": "Redundant copy \u2014 .letter-spacing-05 is also in custom-spectrum.css, which all eight bundle entries import, so this block shadows a global definition rather than supplying a missing one. Measured 2026-08-30.",
        "src/core/ui/components/wizard/ConfigurationSummary.tsx::text-gray-600": "Redundant copy \u2014 .text-gray-600 is also in custom-spectrum.css, which all eight bundle entries import, so this block shadows a global definition rather than supplying a missing one. Measured 2026-08-30.",
        "src/core/ui/components/wizard/ConfigurationSummary.tsx::text-gray-700": "Redundant copy \u2014 .text-gray-700 is also in custom-spectrum.css, which all eight bundle entries import, so this block shadows a global definition rather than supplying a missing one. Measured 2026-08-30.",
        "src/core/ui/components/wizard/ConfigurationSummary.tsx::text-green-600": "Redundant copy \u2014 .text-green-600 is also in custom-spectrum.css, which all eight bundle entries import, so this block shadows a global definition rather than supplying a missing one. Measured 2026-08-30.",
        "src/core/ui/components/wizard/ConfigurationSummary.tsx::text-orange-600": "Redundant copy \u2014 .text-orange-600 is also in custom-spectrum.css, which all eight bundle entries import, so this block shadows a global definition rather than supplying a missing one. Measured 2026-08-30.",
        "src/core/ui/components/wizard/ConfigurationSummary.tsx::text-sm": "Redundant copy \u2014 .text-sm is also in custom-spectrum.css, which all eight bundle entries import, so this block shadows a global definition rather than supplying a missing one. Measured 2026-08-30.",
        "src/core/ui/components/wizard/ConfigurationSummary.tsx::text-uppercase": "Redundant copy \u2014 .text-uppercase is also in custom-spectrum.css, which all eight bundle entries import, so this block shadows a global definition rather than supplying a missing one. Measured 2026-08-30.",
        "src/core/ui/components/wizard/ConfigurationSummary.tsx::text-xs": "Redundant copy \u2014 .text-xs is also in custom-spectrum.css, which all eight bundle entries import, so this block shadows a global definition rather than supplying a missing one. Measured 2026-08-30.",
        "src/features/eds/ui/components/VerifiedField.tsx::text-red-500": "Redundant copy \u2014 .text-red-500 was added to custom-spectrum.css when this block was found to be its only definition (see the comment at that sheet's line 6177). The block copy was never removed. Measured 2026-08-30.",
        "src/features/project-creation/ui/wizard/WizardContainer.tsx::backward": "Redundant copy \u2014 .backward is also in custom-spectrum.css and wizard.css. Measured 2026-08-30.",
        "src/features/project-creation/ui/wizard/WizardContainer.tsx::forward": "Redundant copy \u2014 .forward is also in custom-spectrum.css and wizard.css. Measured 2026-08-30."
    },
    "_styleBlockLeaks_note": "Thirteen classes defined in a component <style> block AND used by another component. NONE is broken: every one is also defined in custom-spectrum.css, which all eight bundle entries import \u2014 so these are redundant copies shadowing a global sheet, not missing styles. Deleting them is not free: the global copies carry !important in places and the inline ones do not, so removal can change which declaration wins. That needs webview-visual-baseline and belongs to PL-21. This ledger stops the set growing meanwhile.",
    "staticInlineStyleCeiling": 3,
    "dynamicInlineStyleCeiling": 20,
    "_inlineStyleCeilings_note": "Pinned 2026-08-30. The per-file cap of five static inline styles in inline-styles.test.ts bounds any ONE file and says nothing about the total; these pin the aggregate so the direction of travel is one way. Static and dynamic are pinned separately so removing a static one cannot be paid for by adding a dynamic one.",
    "emptyLiteralProps": {},
    "_emptyLiteralProps_note": "Empty ARRAY/OBJECT literals passed as JSX props. Seeded EMPTY on 2026-08-31: measured zero in src/, so this is a flat ban rather than a shrink-only ledger. The dangerous shape is `prop={[]}` \u2014 a new reference every render feeding an effect that sets state, which the root CLAUDE.md records as having already happened here. The fix is a module-scope `const EMPTY: never[] = []`. Non-empty literals (34) and inline arrows (90) are NOT covered: whether they reach a dependency array cannot be decided without following the prop into the receiving hook."
}
