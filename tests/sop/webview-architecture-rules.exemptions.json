{
    "_what": "ADR-017 enforcement ledger (WEBVIEW ONLY). Same contract as ADR-015's: an entry is a KNOWN pre-ruling violation with its reason; the test fails on an unlisted violation, on a listed entry that no longer violates, and on an entry without a reason. Moved here 2026-08-29 (PL-17) from the ADR-015 ledger, where a pure React rule had been enforced under a document that mentions React zero times. `bundleStylesheets` rows are ADR-017 §6 violations found by webview-stylesheet-bundles.test.ts. `dynamicClassSiteCeiling` is a RATCHET, not an exemption: it counts className expressions this check cannot read statically (template literals), so it measures how much of the surface is NOT covered. It may only go down.",
    "hookRefs": {
        "src/core/ui/hooks/useActivateOnKey.ts": "flagged by the coarse inline-literal detector — adjudicate: legitimate default value vs unstable reference",
        "src/core/ui/hooks/useFocusOnMount.ts": "flagged by the coarse inline-literal detector — adjudicate: legitimate default value vs unstable reference",
        "src/features/components/ui/hooks/useStoreDiscovery.ts": "flagged by the coarse inline-literal detector — adjudicate: legitimate default value vs unstable reference",
        "src/features/dashboard/ui/hooks/useDashboardStatus.ts": "flagged by the coarse inline-literal detector — adjudicate: legitimate default value vs unstable reference",
        "src/features/project-creation/ui/components/ConnectStoreStepContent.tsx": "flagged by the coarse inline-literal detector — adjudicate: legitimate default value vs unstable reference"
    },
    "dynamicClassSiteCeiling": 95,
    "_dynamicClassSiteCeiling_note": "151 -> 123 on 2026-08-29 by converting 10 template-literal className expressions to cn(), which this check can read. IMPORTANT: this number counts SITES PER BUNDLE, not distinct files — a shared component in five bundles contributes five. The distinct-site count is far smaller (36 before the conversion, of which 23 were `className={someProp}` pass-throughs whose real class is checked at the CALL site, so never a blind spot). What remains is one genuinely dynamic case (InlineNotice builds `inline-notice--${tone}`) and two helper-call sites, multiplied across bundles. 123 -> 105 on 2026-08-31 by PL-31: with the UI barrels gone, a bundle imports only the components it uses instead of everything the barrel re-exported, so the same dynamic-className sites are multiplied across fewer bundles. The count is sites PER BUNDLE, which is why deleting no component at all moved it. Then 105 -> 95 as the remaining UI barrels went (same cause).",
    "classesDefinedNowhere": {
        "complete": "KEEP — a TEST HANDLE, not a missing rule. No stylesheet defines it and nothing in src/ queries it, but the suite does: BrandGallery-cardState.test.tsx asserts toHaveClass('complete') on the chosen card. Verified 2026-09-08 (PL-20) by reading the asserting line, after a narrower check wrongly cleared `.done` and removing it broke four tests.",
        "config-section-footer": "KEEP — a TEST HANDLE, not a missing rule. No stylesheet defines it and nothing in src/ queries it, but the suite does: ConfigSection.test.tsx queries it to assert the footer is absent, then present. Verified 2026-09-08 (PL-20) by reading the asserting line, after a narrower check wrongly cleared `.done` and removing it broke four tests.",
        "control-panel": "KEEP — a TEST HANDLE, not a missing rule. No stylesheet defines it and nothing in src/ queries it, but the suite does: ControlPanelLayout.test.tsx queries '.control-panel.dashboard-control-panel'. Verified 2026-09-08 (PL-20) by reading the asserting line, after a narrower check wrongly cleared `.done` and removing it broke four tests.",
        "control-panel-secondary-inner": "KEEP — a TEST HANDLE, not a missing rule. No stylesheet defines it and nothing in src/ queries it, but the suite does: ControlPanelLayout.test.tsx queries it three times, including for its maxWidth. Verified 2026-09-08 (PL-20) by reading the asserting line, after a narrower check wrongly cleared `.done` and removing it broke four tests.",
        "dashboard-control-panel": "KEEP — a TEST HANDLE, not a missing rule. No stylesheet defines it and nothing in src/ queries it, but the suite does: ControlPanelLayout.test.tsx passes it as className and queries the pair. Verified 2026-09-08 (PL-20) by reading the asserting line, after a narrower check wrongly cleared `.done` and removing it broke four tests.",
        "page-header": "KEEP — a TEST HANDLE, not a missing rule. No stylesheet defines it and nothing in src/ queries it, but the suite does: PageHeader.test.tsx queries '.border-b.bg-gray-75.page-header'. Verified 2026-09-08 (PL-20) by reading the asserting line, after a narrower check wrongly cleared `.done` and removing it broke four tests.",
        "done": "KEEP. Not a missing rule — a STATE MARKER the suite reads. The CSS that drew the done tick (.sum-row.done .sum-label::before) was deliberately removed in 99e2aed08 when the marker became a Spectrum CheckmarkCircle, so the class styles nothing. But BuildYourProjectSummary's and CommerceStep's tests assert on it as the handle for 'this row is done'. Removing it on 2026-09-08 broke four tests, which is the reason this entry now says keep rather than fix. Triaged PL-20."
    },
    "_classesDefinedNowhere_note": "Classes a component asks for that NO stylesheet in that bundle defines — and that no stylesheet anywhere in src/ defines either. A different defect from bundleStylesheets, with a different fix: for these, either the rule was never written or the markup is dead. Seeded at 21 on 2026-08-29 so the set cannot GROW while it is triaged; the triage itself is PL-20. Two are visible bugs already (.text-red-500 on an error icon, .text-orange-700 on warning text) — they render with no colour. Note .text-red-500 IS defined, but inside VerifiedField's inline <style> block, so it exists only where that component is mounted; the scanner counts style-block classes as defined within their own bundle, which is why this reports only the bundles VerifiedField does not reach.",
    "messageChannelOwners": {
        "src/core/ui/utils/WebviewClient.ts": "THE singleton channel — the shared client all eight bundles talk through. The sidebar joined them on 2026-09-08 (PL-19); before that it acquired its own and was listed below."
    },
    "importantCeiling": 1769,
    "_importantCeiling_note": "ADR-018 measured 1,866 removable uses when it was written; the count had drifted to 1,969 by 2026-08-30, which is why it is pinned rather than left as advice. Migration is PL-21 and is not authorised yet — this only stops the number growing back. LOWERED to 1,965 on 2026-09-08: the check counted raw matches, so seven of those 1,969 were the word !important inside COMMENTS explaining the problem. It now strips comments before counting, which is why the pin falls without a single declaration being removed. 1965 -> 1923 on 2026-09-08: 47 selector parts targeting Spectrum by a BARE class name were deleted (19 whole rules, 28 trimmed from surviving rules). Spectrum ships HASHED class names — the real DOM class is o7Xu8a_spectrum-Button — so `.spectrum-Button` matched nothing and had been inert since before 3.16. Proved dead by an EMPTY visual diff across 2,700 elements: removing them changed nothing because they were doing nothing. 1923 -> 1782 on 2026-09-09: 141 !important rode along with the 32 dead rules. 1782 -> 1769 on 2026-09-09: nine selectors were declared two or three times in one sheet and were merged into one rule each; 13 !important declarations were duplicates of a property the surviving copy already set, and went with them. No declaration that was WINNING was removed.",
    "styleBlockLeaks": {
        "src/features/eds/ui/components/VerifiedField.tsx::text-red-500": "Redundant copy — .text-red-500 was added to custom-spectrum.css when this block was found to be its only definition (see the comment at that sheet's line 6177). The block copy was never removed. Measured 2026-08-30.",
        "src/features/project-creation/ui/wizard/WizardContainer.tsx::backward": "Redundant copy — .backward is also in custom-spectrum.css and wizard.css. Measured 2026-08-30.",
        "src/features/project-creation/ui/wizard/WizardContainer.tsx::forward": "Redundant copy — .forward is also in custom-spectrum.css and wizard.css. Measured 2026-08-30."
    },
    "_styleBlockLeaks_note": "Thirteen classes defined in a component <style> block AND used by another component. NONE is broken: every one is also defined in custom-spectrum.css, which all eight bundle entries import — so these are redundant copies shadowing a global sheet, not missing styles. Deleting them is not free: the global copies carry !important in places and the inline ones do not, so removal can change which declaration wins. That needs webview-visual-baseline and belongs to PL-21. This ledger stops the set growing meanwhile.",
    "staticInlineStyleCeiling": 3,
    "dynamicInlineStyleCeiling": 20,
    "_inlineStyleCeilings_note": "Pinned 2026-08-30. The per-file cap of five static inline styles in inline-styles.test.ts bounds any ONE file and says nothing about the total; these pin the aggregate so the direction of travel is one way. Static and dynamic are pinned separately so removing a static one cannot be paid for by adding a dynamic one.",
    "godFileTopLevelRules": 556,
    "_godFileTopLevelRules_note": "Top-level rules in custom-spectrum.css. The CONTROL for featureRulesInGlobalSheet: if that falls while this does not, rules were relabelled rather than moved out. Measured 920 on 2026-09-08 at the start of the CSS migration (.rptc/plans/css-architecture-migration). Target is the utility count, ~235 — at which point the file is a utility sheet, not a god file.",
    "featureRulesInGlobalSheet": 321,
    "_featureRulesInGlobalSheet_note": "Rules in custom-spectrum.css whose first class token is NOT a utility prefix — i.e. feature CSS living in the sheet all eight bundles import. Step 2's progress metric; target 0. The utility prefix list lives in the test rather than here so the measurement and its definition cannot drift apart. 685 of 920 on 2026-09-08, across 102 families; 23 families carry 10+ rules and cover 464 of them. 685 -> 660 on 2026-09-08: the .prerequisite-* family (25 rules) moved to src/features/prerequisites/ui/styles/prerequisites.css, imported by the wizard entry, which is the only one whose graph reaches PrerequisitesStep. Verified by an EMPTY visual diff across 2,700 elements. 660 -> 629 on 2026-09-09: .brand-* (18) and .expandable-* (13) moved together to src/features/project-creation/ui/styles/brand-cards.css. They were moved together because the completeness check refused to move .brand-* alone. CORRECTED 2026-09-09: that refusal was a FALSE one. The two rules that blocked it (`.expandable-brand-card.expanded .brand-card-header` and its :hover sibling) are OWNED by .expandable-*, not by .brand-*, so they were never part of the brand move; .expandable-* itself was blocked by nothing at all. Either family could have moved on its own. The sheet they share is still their right home — both are used only by BrandGallery/WelcomeStep and both reach the wizard entry alone — so the outcome stands and the reason given for it did not. Empty diff across 2,700 elements. 629 -> 611 on 2026-09-09: .ai-* (18) moved to src/features/dashboard/ui/styles/ai.css, imported by all THREE dashboard-feature entries (main, aiSurface, integrationsSurface) whose graphs reach a component using it. Empty diff across 2,700 elements. 611 -> 599 on 2026-09-09: .datapack-* (12) moved to src/features/data-installer/ui/styles/datapack.css, imported by the wizard and dataInstaller entries. These rules were in custom-spectrum's UNLAYERED carve-out and the mover emitted them unlayered, which is the layer-preservation working in the other direction. Empty diff. 599 -> 576 on 2026-09-09: .int-* (12) to integration-flow.css and .sum-* (11) to build-summary.css, both wizard-only. Empty diff. 576 -> 564 on 2026-09-09: .eventing-* (8) to the dashboard feature's eventing.css, imported ONLY by the integrations entry — the single entry reaching EventingSection — and .progress-* (4) appended to the existing prerequisites.css. Empty diff. 564 -> 532 on 2026-09-09: 32 DEAD rules deleted rather than moved — eight .architecture-* sub-families and .project-button-*, none of which any component names. .architecture-badge was KEPT: it styles the 'Coming Soon' badge in BrandGallery. 532 -> 496 on 2026-09-09: .project-* (36, card and row) to src/features/projects-dashboard/ui/styles/project-cards.css, imported by the projectsList entry ALONE. The family had looked cross-cutting at 33 users across 6 bundles; that was a bare `project-` prefix match catching file paths and prose. Decomposed by sub-family with a control it is one bundle. Empty diff. 496 -> 468 on 2026-09-09: .dashboard-* (28) to src/features/dashboard/ui/styles/dashboard.css, imported by the dashboard entry ALONE — the only one of the eight whose graph reaches ActionGrid, DashboardTile, DashboardStatusHeader or ProjectDashboardScreen. Empty resting diff across 2,700 elements AND an empty INTERACTION diff across 168 cells, which this family needed because six of its rules carry :hover / :focus / :focus-visible. 468 -> 416 on 2026-09-09: .intflow-* (52, the largest family) to src/features/project-creation/ui/styles/add-integration-flow.css, imported by BOTH entries that render the Add Integration modal — wizard and integrations. It had been blocked since the migration began by a single `.manage-apis-body .intflow-api-picker` rule, which the completeness check counted as a leftover; that rule is OWNED by .manage-* and stays in the god file. Verified in the built bundles: 6 occurrences in each of those two, 1 in the other six (the .manage-* rule the god file still carries). Empty resting diff across 2,700 elements and empty interaction diff across 168 cells. 416 -> 378 on 2026-09-09: .timeline-* (19) and .wizard-* (19) to src/features/project-creation/ui/styles/wizard-timeline.css, wizard entry only. GENUINE entanglement, unlike brand/expandable: nine rules led by `.wizard-timeline-column` style `.timeline-*` descendants, so the rail's collapsed appearance is written half in each family. The `@media (max-width: 1280px)` rail-collapse block was moved BY HAND, whole, because --move refuses a family with a conditional rule. Appending it BEFORE the wizard base rules reversed the document order of two equal-specificity `.wizard-timeline-column` rules, the rail stopped collapsing and 258 elements moved; reordered to match the god file and the diff went empty. 378 -> 377 on 2026-09-09: not a move — .node-version-line was declared TWICE in custom-spectrum.css and the two were merged into one rule. Eight more such merges happened in the feature sheets, which this count does not see. 377 -> 342 on 2026-09-09: .integration-* (35) to src/core/ui/styles/integration-cards.css, imported by the wizard, integrations and dataInstaller entries — the only three whose graphs reach IntegrationCard or DatapackDetailPanel. In core/ui/styles because the component is in core/ui/components/integrations, NOT because it is global; wizard.css beside it reaches four entries and is the precedent. Empty resting diff and empty interaction diff. 342 -> 321 on 2026-09-09: .modal-* (21) to src/core/ui/styles/modal.css, imported by the five entries that render the shared Modal — wizard, dashboard, integrations, aiOverview, dataInstaller. configure, sidebar and projectsList do not. `.modal-body:has(.manage-apis-body)` travelled with it: its first class decides ownership and it styles the modal body. Empty resting and interaction diffs.",
    "vendorLayerBundles": 0,
    "_vendorLayerBundles_note": "A FLOOR, not a ceiling — it counts up. How many of the eight webview bundles ship vendor CSS in a cascade layer below ours (ADR-018 §1). 0 of 8 on 2026-09-08. This is also PL-33's one unenforced convention: 'vendor CSS sits in the lowest cascade layer' cannot be enforced while it is true of zero bundles, and becomes enforceable at 8. Gated on step 2 — until custom-spectrum is split, all eight entries import it and there is no seam to flip one surface through.",
    "capturedProperties": 26,
    "_capturedProperties_note": "A FLOOR. How many CSS properties the visual fingerprint records (.claude/skills/webview-visual-baseline/capture.js PROPS). The net may get sharper and may never get blunter: a migration verified by a coarsening net is unverified and looks identical to a verified one. 16 originally; 23 after the ADR-018 self-audit; 26 on 2026-09-08 when deleting a letter-spacing rule produced an empty diff that proved nothing.",
    "interactionCells": 168,
    "_interactionCells_note": "A FLOOR. Cells captured by capture-interactions.js — 42 interactive elements across 8 surfaces at rest/hover/focus/active. Measured 2026-09-08. Known gap kept deliberately visible: the sidebar's 6 interactive elements respond to NO forced pseudo-state, so its interaction styling is invisible to this instrument and a clean sidebar row is not evidence.",
    "bundleStylesheets": {}
}
