{
    "_what": "Every MCP tool, and whether it was REVIEWED for the credentials rule: a tool that needs credentials pre-flights and returns a structured `needsAuth` handoff rather than erroring, so the agent can drive sign-in and retry.\n\nWHY A LEDGER RATHER THAN A CHECK OVER THE CODE. There is no marker that says a tool touches credentials. Compliance is reached three ways \u2014 runGuards, a bespoke pre-flight, or nothing because none is needed \u2014 and no scan separates them. The readOnly field solved the same problem by making the AUTHOR declare it; this does the same with a row instead of a type, because only 48 of the 114 tools have a descriptor to put a field on.\n\nWHAT THE CHECK BUYS TODAY: a NEW tool has no row, so the build fails and the question gets asked at authoring time. That is stopping the bleeding. Draining the pool is the `unreviewedCeiling`, which may only fall.\n\nA verdict is one of: NEEDS-AUTH (and how it hands off) / NO-AUTH (why not) / UNREVIEWED. Replace UNREVIEWED with a real verdict and a reason; never delete a row for a tool that still exists.",
    "_seeded": "2026-08-31 \u2014 114 tools, all UNREVIEWED. Nobody's count of the surface was right before this: the tool-verdicts skill says 107 and an earlier pass in this session said 102, both missing dataInstallerDescriptors.ts (8) and statusDescriptors.ts (4).",
    "unreviewedCeiling": 0,
    "tools": {
        "add_console_apis": "NEEDS-AUTH adobe \u2014 runs runGuards (ensureAdobeIOAuth + org-mismatch + developer role). BREACH OF SHAPE, systemic to descriptor tools: runGuards returns {error, code: AUTH_REQUIRED} and defaultShape renders that as the STRING 'Error: Adobe sign-in required. [AUTH_REQUIRED]'. AUTH_REQUIRED appears ZERO times in src/features/ai \u2014 the webview translates it, the MCP path does not \u2014 so the agent gets prose to parse instead of a needsAuth handoff (consoleApiHandlers)",
        "add_integration": "NEEDS-AUTH adobe \u2014 reaches guardOrBlock/runGuards (ensureAdobeIOAuth + org mismatch + developer role) in appBuilderComponentHandlers. Shape breach is systemic to descriptor tools: defaultShape renders any failure as an 'Error: \u2026 [CODE]' string, not a handoff. Guards run INSIDE the progress wrapper, deliberately \u2014 a cold `aio config get` left users staring at nothing when they ran first",
        "apply_updates": "NEEDS-AUTH github \u2014 hands ctx.context.secrets to applyUpdatesHeadless, which pulls component updates from GitHub Releases. Directly registered, so a failure is whatever applyUpdatesHeadless returns rather than the descriptor string",
        "check_datapack_service": "NEEDS-AUTH adobe \u2014 COMPLIES, and is the reference implementation: dataInstallerHandlers branches on context.panel and the HEADLESS branch returns refuse(..., AUTH_REQUIRED, {needsAuth:'adobe'}) instead of prompting. Its docblock states the reason \u2014 ensureAdobeIOAuth's notification is right from a webview and wrong from an agent tool",
        "check_github_app": "NEEDS-AUTH github+dalive \u2014 checkGitHubAppHandler calls getGitHubServices(context.context.secrets) and tryCreateDaLiveTokenProvider. Shape breach: failures arrive via defaultShape as an 'Error: \u2026 [CODE]' string, not a handoff",
        "check_mesh": "NEEDS-AUTH adobe \u2014 calls ensureAuthenticated (mesh/handlers/shared.ts). DEFECT, not just a shape breach: that guard calls vscode.window.showWarningMessage with an 'Open Dashboard' button and AWAITS the click, so an unauthenticated agent call puts a notification on the user's window and blocks the tool until someone dismisses it. This is the exact hazard dataInstallerHandlers documented and designed around \u2014 'wrong from an agent tool, where it would block the tool until someone clicks'. The mesh handlers did not get that treatment",
        "check_prerequisites": "NO-AUTH \u2014 probes local tool versions by running commands; no credential",
        "check_repo_readiness": "NEEDS-AUTH github \u2014 checkRepoReadinessHandler calls getGitHubServices(context.context.secrets) for file operations. Same defaultShape string-not-handoff breach",
        "cleanup_dalive_site": "NEEDS-AUTH adobe \u2014 DA.live operations built from the Adobe IMS token; buildDaLiveOps returns null when it is missing/expired and the caller turns that into the structured NEEDS_ADOBE handoff. COMPLIES",
        "configure_project": "NO-AUTH for credentials \u2014 writes project config and SPLITS secrets into SecretStorage. Registry keys of type `password` return a `needsUser` handoff, which is the right shape: a person must type a secret, and it must never ride a tool argument",
        "connect_dalive": "NEEDS-AUTH dalive \u2014 COMPLIES via `needsUser`, which is the correct shape here and not `needsAuth`: the DA.live token comes from a bookmarklet the user clicks while signed in to da.live and then pastes. No agent can drive that, so the tool hands back naming the command that opens the setup",
        "create_adobe_project": "NEEDS-AUTH adobe \u2014 COMPLIES: returns the structured NEEDS_ADOBE_AUTH handoff ({needsAuth:'adobe', message naming get_auth_status then sign_in}) rather than erroring (adobeResourceTools.ts)",
        "create_adobe_workspace": "NEEDS-AUTH adobe \u2014 COMPLIES: returns the structured NEEDS_ADOBE_AUTH handoff ({needsAuth:'adobe', message naming get_auth_status then sign_in}) rather than erroring (adobeResourceTools.ts)",
        "create_event_provider": "NEEDS-AUTH adobe \u2014 COMPLIES: returns the structured NEEDS_ADOBE_AUTH handoff ({needsAuth:'adobe', message naming get_auth_status then sign_in}) rather than erroring (eventProviderTools.ts)",
        "create_event_registration": "NEEDS-AUTH adobe \u2014 COMPLIES: returns the structured NEEDS_ADOBE_AUTH handoff ({needsAuth:'adobe', message naming get_auth_status then sign_in}) rather than erroring (eventProviderTools.ts)",
        "create_github_repo": "NEEDS-AUTH github \u2014 COMPLIES: returns the structured NEEDS_GITHUB handoff naming get_auth_status then sign_in(provider:'github')",
        "create_project": "NEEDS-AUTH github+dalive \u2014 COMPLIES: pre-flights GitHub and DA.live auth and returns a needsAuth handoff (the file's own docblock names this as one of the two conventions `needsUser` generalises)",
        "delete_adobe_project": "NEEDS-AUTH adobe \u2014 COMPLIES: returns the structured NEEDS_ADOBE_AUTH handoff ({needsAuth:'adobe', message naming get_auth_status then sign_in}) rather than erroring (adobeResourceTools.ts)",
        "delete_ai_prompt": "NO-AUTH \u2014 removes a prompt from settings/project",
        "delete_event_provider": "NEEDS-AUTH adobe \u2014 COMPLIES: returns the structured NEEDS_ADOBE_AUTH handoff ({needsAuth:'adobe', message naming get_auth_status then sign_in}) rather than erroring (eventProviderTools.ts)",
        "delete_event_registration": "NEEDS-AUTH adobe \u2014 COMPLIES: returns the structured NEEDS_ADOBE_AUTH handoff ({needsAuth:'adobe', message naming get_auth_status then sign_in}) rather than erroring (eventProviderTools.ts)",
        "delete_github_repo": "NEEDS-AUTH github \u2014 COMPLIES: returns the structured NEEDS_GITHUB handoff naming get_auth_status then sign_in(provider:'github')",
        "delete_mesh": "NEEDS-AUTH adobe \u2014 same ensureAuthenticated path and the same await-a-click defect as check_mesh",
        "delete_page": "NEEDS-AUTH dalive \u2014 COMPLIES: returns the structured NEEDS_DALIVE / NEEDS_GITHUB handoff naming get_auth_status then sign_in",
        "delete_project": "NO-AUTH \u2014 projectDeletionService removes the local project directory and state; no service, no token",
        "deploy_integration": "NEEDS-AUTH adobe \u2014 reaches guardOrBlock/runGuards (ensureAdobeIOAuth + org mismatch + developer role) in appBuilderComponentHandlers. Shape breach is systemic to descriptor tools: defaultShape renders any failure as an 'Error: \u2026 [CODE]' string, not a handoff",
        "deploy_mesh": "NEEDS-AUTH adobe \u2014 deployMeshWithFeedback takes the auth manager and secret storage. NEAR MISS, not an oversight: the handler owns a BLOCK_MESSAGE map whose auth entry reads 'Adobe sign-in required. Sign in (sign_in), then retry.' and whose comment says why \u2014 'the tool has no UI to recover inline'. It is agent-facing recovery prose returned as {success:false, error}, so defaultShape renders a string. Deliberate and useful, still not the structured handoff",
        "discover_store_structure": "NEEDS-AUTH commerce \u2014 edsHandlers reaches the Commerce backend. PaaS discovery additionally needs an admin username and password, which is a needsUser handoff rather than a needsAuth one. Same defaultShape breach",
        "edit_project": "NO-AUTH \u2014 ALWAYS a handoff, never a dispatch: it opens the creation wizard and returns `needsUser`. Its own comment says nothing is changed by the call. No credential path",
        "export_project_settings": "NO-AUTH \u2014 writes the project's settings to a file the user chooses; no service",
        "find_datapacks": "NEEDS-AUTH adobe \u2014 COMPLIES, and is the reference implementation: dataInstallerHandlers branches on context.panel and the HEADLESS branch returns refuse(..., AUTH_REQUIRED, {needsAuth:'adobe'}) instead of prompting. Its docblock states the reason \u2014 ensureAdobeIOAuth's notification is right from a webview and wrong from an agent tool",
        "find_storefront_name_mismatches": "NEEDS-AUTH dalive \u2014 needs a DA.live credential via createDaLiveServiceTokenProvider. DEVIATION IN SHAPE, not in spirit: siteAccessManagerHeadless returns a domain status `{status:'no_credential'}` rather than the `needsAuth` shape. It is structured and deliberately distinguishes 'not signed in' from 'refused' \u2014 its own comment records that merging the two once sent users to the Debug Logs instead of to sign-in",
        "get_agent_trace": "NO-AUTH \u2014 reads local session trace files written by this extension (fs + agentTraceSink)",
        "get_auth_status": "NO-AUTH by definition \u2014 this is the tool that REPORTS auth state; requiring auth to ask about auth would be circular. It is the tool every needsAuth handoff points at",
        "get_block_authoring_shape": "NO-AUTH \u2014 derives authoring markup from local block source; does not call resolveCredentials",
        "get_commerce_endpoints": "NO-AUTH \u2014 derives endpoints from the project's own persisted state and config generator; no network, no token",
        "get_component_config": "NO-AUTH \u2014 reads .demo-builder.json or a .env inside the project dir and MASKS secret values so they never enter the transcript; no resolveCredentials",
        "get_component_requirements": "NO-AUTH \u2014 reads the bundled components.json catalog",
        "get_current_project": "NO-AUTH \u2014 reads the persisted project record via resolveProjectStatus; imports are the result helper and HandlerContext only, no service and no token",
        "get_datapack": "NEEDS-AUTH adobe \u2014 COMPLIES, and is the reference implementation: dataInstallerHandlers branches on context.panel and the HEADLESS branch returns refuse(..., AUTH_REQUIRED, {needsAuth:'adobe'}) instead of prompting. Its docblock states the reason \u2014 ensureAdobeIOAuth's notification is right from a webview and wrong from an agent tool",
        "get_datapack_activity": "NEEDS-AUTH adobe \u2014 COMPLIES, and is the reference implementation: dataInstallerHandlers branches on context.panel and the HEADLESS branch returns refuse(..., AUTH_REQUIRED, {needsAuth:'adobe'}) instead of prompting. Its docblock states the reason \u2014 ensureAdobeIOAuth's notification is right from a webview and wrong from an agent tool",
        "get_datapack_import_status": "NEEDS-AUTH adobe \u2014 importHandlers returns {success:false, error:'Adobe sign-in is required.', code:AUTH_REQUIRED} with NO needsAuth marker, unlike its sibling dataInstallerHandlers which adds one. Same file also routes a provisioned secret into SecretStorage. Shape breach",
        "get_datapack_import_target": "NEEDS-AUTH adobe \u2014 importHandlers returns {success:false, error:'Adobe sign-in is required.', code:AUTH_REQUIRED} with NO needsAuth marker, unlike its sibling dataInstallerHandlers which adds one. Same file also routes a provisioned secret into SecretStorage. Shape breach",
        "get_integration_install_status": "NO-AUTH \u2014 resolves the component and reads its persisted install status; no guard, no service",
        "get_project": "NO-AUTH \u2014 reads .demo-builder.json off disk; does not call resolveCredentials",
        "get_project_status": "NO-AUTH \u2014 it calls isAuthenticated() to REPORT auth state, which is the opposite of requiring it. Reads persisted project state otherwise",
        "get_project_urls": "NO-AUTH \u2014 derives URLs from persisted project state",
        "get_settings": "NO-AUTH for credentials \u2014 reads/writes VS Code settings. Uses the `needsUser` handoff for settings the extension collects on its own form, which is the right shape: a person must type them, not a sign-in the agent can drive",
        "get_site_access": "NEEDS-AUTH dalive \u2014 needs a DA.live credential via createDaLiveServiceTokenProvider. DEVIATION IN SHAPE, not in spirit: siteAccessManagerHeadless returns a domain status `{status:'no_credential'}` rather than the `needsAuth` shape. It is structured and deliberately distinguishes 'not signed in' from 'refused' \u2014 its own comment records that merging the two once sent users to the Debug Logs instead of to sign-in",
        "get_store_structure": "NEEDS-AUTH commerce \u2014 storeStructureHandler reads the project's Commerce backend using stored admin credentials; its own comment records a project whose migrated admin password reported 'no credentials saved'. Same defaultShape breach",
        "install_integration": "NEEDS-AUTH adobe \u2014 handleInstallAppBuilderComponent (appManagementInstallHandlers) runs the guards. Shape breach is systemic to descriptor tools: defaultShape renders any failure as an 'Error: \u2026 [CODE]' string, not a handoff",
        "install_prerequisite": "NO-AUTH \u2014 installs a local tool via the shell; no credential path in the handler",
        "list_adobe_projects": "NEEDS-AUTH adobe \u2014 COMPLIES: returns the structured NEEDS_ADOBE_AUTH handoff ({needsAuth:'adobe', message naming get_auth_status then sign_in}) rather than erroring; org-targeted per call via withOrgContext (adobeTools.ts)",
        "list_ai_prompts": "NO-AUTH \u2014 reads prompts from settings and the project",
        "list_blocks": "NO-AUTH \u2014 lists directories under the storefront blocks/ folder; does not call resolveCredentials",
        "list_components": "NO-AUTH \u2014 reads bundled catalog JSON (components.json, app-builder-components.json, stacks) shipped in the extension; no service, no token",
        "list_console_apis": "NEEDS-AUTH adobe \u2014 runs runGuards (ensureAdobeIOAuth + org-mismatch + developer role). BREACH OF SHAPE, systemic to descriptor tools: runGuards returns {error, code: AUTH_REQUIRED} and defaultShape renders that as the STRING 'Error: Adobe sign-in required. [AUTH_REQUIRED]'. AUTH_REQUIRED appears ZERO times in src/features/ai \u2014 the webview translates it, the MCP path does not \u2014 so the agent gets prose to parse instead of a needsAuth handoff (consoleApiHandlers) \u2014 a READ, but still guarded",
        "list_content": "NEEDS-AUTH dalive \u2014 COMPLIES: returns the structured NEEDS_DALIVE / NEEDS_GITHUB handoff naming get_auth_status then sign_in",
        "list_dalive_sites": "NEEDS-AUTH adobe \u2014 DA.live operations built from the Adobe IMS token; buildDaLiveOps returns null when it is missing/expired and the caller turns that into the structured NEEDS_ADOBE handoff. COMPLIES",
        "list_datapack_data_types": "NEEDS-AUTH adobe \u2014 COMPLIES, and is the reference implementation: dataInstallerHandlers branches on context.panel and the HEADLESS branch returns refuse(..., AUTH_REQUIRED, {needsAuth:'adobe'}) instead of prompting. Its docblock states the reason \u2014 ensureAdobeIOAuth's notification is right from a webview and wrong from an agent tool",
        "list_datapack_export_items": "NEEDS-AUTH adobe \u2014 importHandlers returns {success:false, error:'Adobe sign-in is required.', code:AUTH_REQUIRED} with NO needsAuth marker, unlike its sibling dataInstallerHandlers which adds one. Same file also routes a provisioned secret into SecretStorage. Shape breach",
        "list_datapack_import_scopes": "NEEDS-AUTH adobe \u2014 importHandlers returns {success:false, error:'Adobe sign-in is required.', code:AUTH_REQUIRED} with NO needsAuth marker, unlike its sibling dataInstallerHandlers which adds one. Same file also routes a provisioned secret into SecretStorage. Shape breach",
        "list_demo_packages": "NO-AUTH \u2014 reads bundled catalog JSON (components.json, app-builder-components.json, stacks) shipped in the extension; no service, no token",
        "list_event_providers": "NEEDS-AUTH adobe \u2014 COMPLIES: returns the structured NEEDS_ADOBE_AUTH handoff ({needsAuth:'adobe', message naming get_auth_status then sign_in}) rather than erroring (eventProviderTools.ts)",
        "list_github_repos": "NEEDS-AUTH github \u2014 COMPLIES: returns the structured NEEDS_GITHUB handoff naming get_auth_status then sign_in(provider:'github')",
        "list_installed_datapacks": "NEEDS-AUTH adobe \u2014 COMPLIES, and is the reference implementation: dataInstallerHandlers branches on context.panel and the HEADLESS branch returns refuse(..., AUTH_REQUIRED, {needsAuth:'adobe'}) instead of prompting. Its docblock states the reason \u2014 ensureAdobeIOAuth's notification is right from a webview and wrong from an agent tool",
        "list_orgs": "NEEDS-AUTH adobe \u2014 COMPLIES: returns the structured NEEDS_ADOBE_AUTH handoff ({needsAuth:'adobe', message naming get_auth_status then sign_in}) rather than erroring; org-targeted per call via withOrgContext (adobeTools.ts)",
        "list_projects": "NO-AUTH \u2014 reads project directories under the projects root; does not call resolveCredentials",
        "list_stacks": "NO-AUTH \u2014 reads bundled catalog JSON (components.json, app-builder-components.json, stacks) shipped in the extension; no service, no token",
        "list_workspaces": "NEEDS-AUTH adobe \u2014 COMPLIES: returns the structured NEEDS_ADOBE_AUTH handoff ({needsAuth:'adobe', message naming get_auth_status then sign_in}) rather than erroring; org-targeted per call via withOrgContext (adobeTools.ts)",
        "migrate_storefront_name": "NEEDS-AUTH dalive \u2014 needs a DA.live credential via createDaLiveServiceTokenProvider. DEVIATION IN SHAPE, not in spirit: siteAccessManagerHeadless returns a domain status `{status:'no_credential'}` rather than the `needsAuth` shape. It is structured and deliberately distinguishes 'not signed in' from 'refused' \u2014 its own comment records that merging the two once sent users to the Debug Logs instead of to sign-in",
        "open_url": "NO-AUTH \u2014 dispatches into dashboardHandlers and hands back with `needsUser` where a person must act; no credential path",
        "open_view": "NO-AUTH \u2014 executes a VS Code command to reveal a panel; zod + result helpers only",
        "promote_block_to_library": "NEEDS-AUTH dalive \u2014 calls resolveCredentials. DEVIATION from the rule: blockToolHandlers.ts THROWS when daLiveToken is missing rather than returning a structured needsAuth handoff. The message does name the recovery ('check get_auth_status, then sign_in with provider:\"dalive\"'), so an agent is not stranded, but it is an error and the rule asks for a handoff",
        "publish_page": "NEEDS-AUTH dalive \u2014 COMPLIES: returns the structured NEEDS_DALIVE / NEEDS_GITHUB handoff naming get_auth_status then sign_in",
        "read_debug_logs": "NO-AUTH \u2014 reads the extension's own output-channel files off disk (fs + path); nothing remote",
        "read_page": "NEEDS-AUTH dalive \u2014 COMPLIES: returns the structured NEEDS_DALIVE / NEEDS_GITHUB handoff naming get_auth_status then sign_in",
        "read_published_page": "NEEDS-AUTH dalive \u2014 COMPLIES: returns the structured NEEDS_DALIVE / NEEDS_GITHUB handoff naming get_auth_status then sign_in",
        "redeploy_integration": "NEEDS-AUTH adobe \u2014 reaches guardOrBlock/runGuards (ensureAdobeIOAuth + org mismatch + developer role) in appBuilderComponentHandlers. Shape breach is systemic to descriptor tools: defaultShape renders any failure as an 'Error: \u2026 [CODE]' string, not a handoff (an alias of handleDeployAppBuilderComponent)",
        "refresh_block_library": "NO-AUTH \u2014 handleRefreshBlockLibraryHeadless refreshes the local block library; no token in the handler",
        "regenerate_ai_files": "NO-AUTH \u2014 regenerates the project's AI bundle from local templates and state; aiHandlers has no credential path at all",
        "reload_window": "NO-AUTH \u2014 executes workbench.action.reloadWindow; zod + result helpers only",
        "remove_block_from_library": "NEEDS-AUTH dalive \u2014 same resolveCredentials path and same throw-instead-of-handoff deviation as promote_block_to_library. NOTE: registerProjectTools' own docblock named only TWO credential-needing tools and missed this one; corrected 2026-08-31",
        "remove_integration": "NEEDS-AUTH adobe \u2014 reaches guardOrBlock/runGuards (ensureAdobeIOAuth + org mismatch + developer role) in appBuilderComponentHandlers. Shape breach is systemic to descriptor tools: defaultShape renders any failure as an 'Error: \u2026 [CODE]' string, not a handoff",
        "rename_integration": "NO-AUTH \u2014 resolves the component then renames it in local project state; no guard, no service. Verified against the full handler body, not a window of it",
        "rename_project": "NO-AUTH \u2014 renameProjectCore renames the local project directory and state",
        "repair_site_configuration": "NEEDS-AUTH dalive \u2014 needs a DA.live credential via createDaLiveServiceTokenProvider. DEVIATION IN SHAPE, not in spirit: siteAccessManagerHeadless returns a domain status `{status:'no_credential'}` rather than the `needsAuth` shape. It is structured and deliberately distinguishes 'not signed in' from 'refused' \u2014 its own comment records that merging the two once sent users to the Debug Logs instead of to sign-in",
        "republish": "NEEDS-AUTH dalive+github \u2014 COMPLIES: requireDaLive/requireGitHub (edsToolGuards.ts) return the `needsAuth` refusal body, which the caller wraps in a tool result",
        "reset_datapack": "NEEDS-AUTH adobe \u2014 importHandlers returns {success:false, error:'Adobe sign-in is required.', code:AUTH_REQUIRED} with NO needsAuth marker, unlike its sibling dataInstallerHandlers which adds one. Same file also routes a provisioned secret into SecretStorage. Shape breach",
        "reset_eds_project": "NEEDS-AUTH dalive \u2014 COMPLIES: its docblock states 'missing auth returns a structured needsAuth handoff'; builds DA.live content operations from the IMS token",
        "restart_demo": "NO-AUTH \u2014 delegates to the demoBuilder.restartDemo command rather than issuing stop+start itself",
        "run_commerce_query": "NO-AUTH \u2014 sends a GraphQL query to the project's own Commerce endpoint with Magento-* store-scope headers. Read-only by design (mutations refused). No user credential; the headers are the scope, not an identity",
        "save_ai_prompt": "NO-AUTH \u2014 writes a prompt to settings/project",
        "select_org": "NEEDS-AUTH adobe \u2014 COMPLIES: returns the structured NEEDS_ADOBE_AUTH handoff ({needsAuth:'adobe', message naming get_auth_status then sign_in}) rather than erroring; org-targeted per call via withOrgContext (adobeTools.ts)",
        "select_project": "NEEDS-AUTH adobe \u2014 COMPLIES: returns the structured NEEDS_ADOBE_AUTH handoff ({needsAuth:'adobe', message naming get_auth_status then sign_in}) rather than erroring; org-targeted per call via withOrgContext (adobeTools.ts)",
        "select_workspace": "NEEDS-AUTH adobe \u2014 COMPLIES: returns the structured NEEDS_ADOBE_AUTH handoff ({needsAuth:'adobe', message naming get_auth_status then sign_in}) rather than erroring; org-targeted per call via withOrgContext (adobeTools.ts)",
        "set_console_apis": "NEEDS-AUTH adobe \u2014 runs runGuards (ensureAdobeIOAuth + org-mismatch + developer role). BREACH OF SHAPE, systemic to descriptor tools: runGuards returns {error, code: AUTH_REQUIRED} and defaultShape renders that as the STRING 'Error: Adobe sign-in required. [AUTH_REQUIRED]'. AUTH_REQUIRED appears ZERO times in src/features/ai \u2014 the webview translates it, the MCP path does not \u2014 so the agent gets prose to parse instead of a needsAuth handoff (consoleApiHandlers)",
        "set_current_project": "NO-AUTH \u2014 switches the active project in local state",
        "set_project_destination": "NEEDS-AUTH adobe \u2014 runs runGuards (ensureAdobeIOAuth + org-mismatch + developer role). BREACH OF SHAPE, systemic to descriptor tools: runGuards returns {error, code: AUTH_REQUIRED} and defaultShape renders that as the STRING 'Error: Adobe sign-in required. [AUTH_REQUIRED]'. AUTH_REQUIRED appears ZERO times in src/features/ai \u2014 the webview translates it, the MCP path does not \u2014 so the agent gets prose to parse instead of a needsAuth handoff (destinationHandlers)",
        "set_project_pinned": "NO-AUTH \u2014 toggles a pin flag in local state",
        "set_setting": "NO-AUTH for credentials \u2014 reads/writes VS Code settings. Uses the `needsUser` handoff for settings the extension collects on its own form, which is the right shape: a person must type them, not a sign-in the agent can drive",
        "set_site_admin": "NEEDS-AUTH dalive \u2014 needs a DA.live credential via createDaLiveServiceTokenProvider. DEVIATION IN SHAPE, not in spirit: siteAccessManagerHeadless returns a domain status `{status:'no_credential'}` rather than the `needsAuth` shape. It is structured and deliberately distinguishes 'not signed in' from 'refused' \u2014 its own comment records that merging the two once sent users to the Debug Logs instead of to sign-in",
        "sign_in": "NO-AUTH by definition \u2014 this is the tool that ESTABLISHES auth; it is the recovery every needsAuth handoff names",
        "start_datapack_export": "NEEDS-AUTH adobe \u2014 importHandlers returns {success:false, error:'Adobe sign-in is required.', code:AUTH_REQUIRED} with NO needsAuth marker, unlike its sibling dataInstallerHandlers which adds one. Same file also routes a provisioned secret into SecretStorage. Shape breach",
        "start_datapack_import": "NEEDS-AUTH adobe \u2014 importHandlers returns {success:false, error:'Adobe sign-in is required.', code:AUTH_REQUIRED} with NO needsAuth marker, unlike its sibling dataInstallerHandlers which adds one. Same file also routes a provisioned secret into SecretStorage. Shape breach",
        "start_demo": "NO-AUTH \u2014 executes the demoBuilder.startDemo VS Code command against the local project",
        "stop_demo": "NO-AUTH \u2014 executes demoBuilder.stopDemo locally",
        "sync_content": "NEEDS-AUTH dalive+github \u2014 COMPLIES: requireDaLive/requireGitHub (edsToolGuards.ts) return the `needsAuth` refusal body, which the caller wraps in a tool result",
        "sync_storefront": "NEEDS-AUTH github \u2014 calls resolveCredentials for a GitHub token to push. DEVIATION: no structured needsAuth handoff; see the two DA.live tools below for the same pattern",
        "update_project_config": "NO-AUTH \u2014 whole-file write to .demo-builder.json or a .env inside the project dir; no resolveCredentials. NOTE for a reviewer: it WRITES secret values to the file (its own comment: 'secrets go to the FILE, never the response'). No credential is needed to do that, but it is a secret-handling path, which is a different question from this rule and worth knowing when auditing one",
        "validate_component_selection": "NO-AUTH \u2014 dispatches into the project-creation registry to validate a component selection; pure logic over the catalog",
        "validate_datapack_import": "NEEDS-AUTH adobe \u2014 importHandlers returns {success:false, error:'Adobe sign-in is required.', code:AUTH_REQUIRED} with NO needsAuth marker, unlike its sibling dataInstallerHandlers which adds one. Same file also routes a provisioned secret into SecretStorage. Shape breach",
        "verify_ai_setup": "NO-AUTH \u2014 inspects the local AI bundle and MCP config",
        "write_page": "NEEDS-AUTH dalive \u2014 COMPLIES: returns the structured NEEDS_DALIVE / NEEDS_GITHUB handoff naming get_auth_status then sign_in"
    }
}
