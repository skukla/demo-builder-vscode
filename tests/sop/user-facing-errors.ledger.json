{
    "_what": "Sites that hand a caught error's own message to a person. The list may only SHRINK: a new one fails the build, and a row for a site that no longer violates fails it too, so clearing one means fixing the code AND deleting its row.",
    "_rule": "A failure returned toward a webview or an agent carries a message written for an SC. Translated means a per-provider formatter, a domain error's own message, or an honest generic - never a caught error's .message passed through.",
    "_the_judgement_this_cannot_make": "Whether the words are THIS EXTENSION'S or a library's is invisible at the call site; it depends on what threw. componentUpdater passes its caught message through and is right to - every error reaching it was thrown by our code with a deliberate sentence. Replacing that with a generic on 2026-09-11 made the product worse and six tests said so.",
    "_seeded": "2026-09-11, 67 sites, owner-decided (see .rptc/plans/error-handling-strategy/overview.md).",
    "rawMessageToUser": {
        "src/commands/claudeCodeFootprint.ts:163": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/commands/diagnosticsChecks.ts:418": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/core/communication/webviewCommunicationManager.ts:423": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/features/ai/server/authTools.ts:163": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/features/ai/server/cloudResourceTools.ts:191": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/features/ai/server/cloudResourceTools.ts:273": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/features/ai/server/createProjectTool.ts:238": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/features/ai/server/createProjectTool.ts:383": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/features/ai/server/deleteProjectTool.ts:73": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/features/ai/server/edsResetTool.ts:181": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/features/ai/server/mcpToolProbe.ts:106": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/features/app-builder/services/appDeployment.ts:353": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/features/components/handlers/componentHandlers.ts:128": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/features/components/handlers/componentHandlers.ts:180": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/features/components/handlers/componentHandlers.ts:221": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/features/components/handlers/componentHandlers.ts:267": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/features/components/handlers/componentHandlers.ts:317": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/features/components/handlers/componentHandlers.ts:372": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/features/components/services/componentManager.ts:105": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/features/dashboard/commands/configure.ts:424": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/features/dashboard/handlers/exportDemoBundleHandler.ts:120": "Reviewed 2026-09-15 (merging develop into the shared-demo branch): NOT translated, deliberately kept for now. The shared-demo branch's own test pins GitHub's failure passing through (\"Failed to download archive: HTTP 404\", worded by githubFileOperations), but a failed fs write in the same try would pass Node's text. Split the two when this row is retired.",
        "src/features/dashboard/handlers/exportDemoBundleHandler.ts:98": "Reviewed 2026-09-15 (merging develop into the shared-demo branch): own words. The try holds only resolveTarget and the save dialog; resolveTarget's only throw is assertPathInsideSync, whose sentences are this extension's.",
        "src/features/dashboard/handlers/projectManagementHandlers.ts:185": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/features/data-installer/handlers/dataInstallerHandlers.ts:171": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/features/data-installer/handlers/exportHandlers.ts:114": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/features/data-installer/handlers/exportHandlers.ts:82": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/features/data-installer/handlers/importHandlers.ts:157": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/features/data-installer/handlers/importHandlers.ts:602": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/features/eds/handlers/daLive/edsDaLiveAuthHandlers.ts:258": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/features/eds/handlers/daLive/edsDaLiveAuthHandlers.ts:88": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/features/eds/handlers/daLive/edsDaLiveAuthHandlers.ts:90": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/features/eds/handlers/edsGitHubHandlers.ts:103": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/features/eds/handlers/edsGitHubHandlers.ts:105": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/features/eds/handlers/edsGitHubHandlers.ts:322": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/features/eds/handlers/edsGitHubHandlers.ts:324": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/features/eds/handlers/importStorefrontZipHandler.ts:117": "Reviewed 2026-09-15 (merging develop into the shared-demo branch): translated one step down. refusalForCreate turns GitHub's \"name already exists\" into a sentence naming the repository, and otherwise quotes only GitHub's own message inside \"GitHub didn't create <repo>: ...\".",
        "src/features/eds/handlers/importStorefrontZipHandler.ts:211": "Reviewed 2026-09-15 (merging develop into the shared-demo branch): own words for the refusals it exists for (\"This zip is neither a demo bundle nor an Edge Delivery storefront.\", \"The bundle's setup file could not be read: ...\", pinned in its tests). A GitHub failure while creating the repository in the same try is not translated.",
        "src/features/eds/handlers/storefrontSetup/storefrontSetupHandlers.ts:422": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/features/eds/handlers/storefrontSetup/storefrontSetupPhases.ts:496": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/features/eds/services/blockCollectionHelpers.ts:297": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/features/eds/services/commerceStoreDiscovery.ts:236": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/features/eds/services/commerceStoreDiscovery.ts:258": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/features/eds/services/configService/configServiceAccess.ts:187": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/features/eds/services/configService/configServiceProbe.ts:260": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/features/eds/services/configService/configServiceProbe.ts:291": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/features/eds/services/configService/configServiceProbe.ts:303": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/features/eds/services/configService/configServiceProbe.ts:355": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/features/eds/services/daLive/daLiveBlockLibraryOperations.ts:189": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/features/eds/services/daLive/daLiveBlockLibraryOperations.ts:548": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/features/eds/services/daLive/daLiveContentCopy.ts:527": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/features/eds/services/daLive/daLiveSourceOperations.ts:158": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/features/eds/services/daLive/daLiveSourceOperations.ts:291": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/features/eds/services/helix/helixService.ts:319": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/features/eds/services/pdp/publishKeyRegistrar.ts:153": "Reviewed 2026-09-15: log-only. The reason goes to `skip`, which writes it to the Debug Logs with what breaks; no caller of `registerPublishKey` reads `reason` (siteConfigRegistrar and the name migration ignore the result, the renewal sweep reads only `registered`). If a caller ever shows `reason` to a person, translate it here first.",
        "src/features/eds/services/refreshBlockLibraryHeadless.ts:171": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/features/eds/services/storefront/storefrontProbe.ts:163": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/features/eds/ui/steps/RepoSelectionInline.tsx:326": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/features/eds/ui/steps/repoSelectionInline.helpers.tsx:127": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/features/mesh/handlers/checkHandler.ts:377": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/features/mesh/handlers/deleteHandler.ts:108": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/features/mesh/services/deployMeshHeadless.ts:252": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/features/mesh/services/meshDeployment.ts:256": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/features/mesh/services/meshVerifier.ts:283": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/features/prerequisites/handlers/installHandler.ts:706": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/features/project-creation/handlers/checkGitHubAppHandler.ts:312": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/features/project-creation/handlers/wizardLifecycleHandlers.ts:181": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/features/project-creation/ui/components/add-demo/useAddDemoFlow.ts:191": "Reviewed 2026-09-15 (merging develop into the shared-demo branch): the webview side. What reaches this catch is WebviewClient's own rejection (a timeout or a closed panel), and an empty message falls back to PROBE_FAILED.",
        "src/features/projects-dashboard/services/projectDeletionService.ts:548": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site.",
        "src/features/projects-dashboard/services/projectDeletionService.ts:585": "Predates the rule (2026-09-11). NOT yet triaged as this extension's own words or a library's - that is the judgement someone makes when they retire the row, and it cannot be read off the call site."
    }
}
