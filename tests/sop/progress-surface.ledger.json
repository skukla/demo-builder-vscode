{
    "_what": "Which surface a long operation opens. Two populations, both enforced by progress-surface.test.ts. 'directProgress' is every vscode.window.withProgress outside the five modules that own the surfaces — each entry says why it still opens its own notification instead of going through withOperationProgress, and the list may only shrink. 'blockingDialogs' is every modal:true dialog — each entry names the decision it gates, because a blocking dialog is for a choice made BEFORE work starts and for nothing else.",

    "directProgress": {
        "src/commands/commandManager.ts:386": "Adobe sign-in, started from the palette, a dashboard button and a wizard step. It is not an operation with stages — one title, one spinner, nothing to report — and the router would have nothing to route.",
        "src/commands/migrateStorefrontNames.ts:157": "A one-off maintenance command run from the palette. R2 already says one notification, which is what this opens; nothing else narrates it.",
        "src/commands/repairSiteConfiguration.ts:78": "Palette maintenance command, reads the site's configuration. One notification is the right surface; it predates the router.",
        "src/commands/repairSiteConfiguration.ts:102": "The republish half of the same palette command. Moving it alone would split one command across two surfaces.",
        "src/features/authentication/handlers/deleteAdobeProjectHandler.ts:134": "Reports stages through stageLine into its own notification. A candidate for the router — it has stages and a screen behind it — and the reason it has not moved is that its teardown reports a step/total shape the ReportStage signature takes differently.",
        "src/features/authentication/services/ensureProjectOrgContext.ts:95": "Telegraphs the browser launch during a forced sign-in. Not an operation: the work is in the browser, and the notification exists so the delay is not silent.",
        "src/features/dashboard/commands/configure.ts:464": "Author-mode switch from the Configure screen. Short, single-step; R5 (a busy state on the control) would be the better answer, which is a UI change, not a routing one.",
        "src/features/dashboard/commands/configure.ts:612": "Republish from the Configure screen, which has no progress modal of its own. Moving it means giving that screen a modal first.",
        "src/features/dashboard/handlers/openUrlHandlers.ts:74": "Covers the second or two an incognito browser takes to launch. There is no operation to narrate and nothing can fail slowly.",
        "src/features/eds/commands/cleanupDaLiveSites.ts:77": "Palette command, reading the site list before anything is chosen. This is the load, not the work.",
        "src/features/eds/commands/cleanupDaLiveSites.ts:180": "The deletion itself, from the palette. R2 says one notification; this is it.",
        "src/features/eds/commands/cleanupDaLiveSites.ts:226": "A timed notification carrying the result of the deletion above. It should be a plain message rather than a progress bar with nothing progressing.",
        "src/features/eds/commands/manageGitHubRepos.ts:91": "Palette command, loading the repository list before anything is chosen.",
        "src/features/eds/commands/manageGitHubRepos.ts:180": "The repository deletion itself, from the palette. One notification is the right surface.",
        "src/features/eds/commands/manageGitHubRepos.ts:214": "A timed notification carrying the deletion result. Same shape as cleanupDaLiveSites:226 and the same thing wrong with it.",
        "src/features/eds/handlers/daLive/daLiveAuthPrompt.ts:469": "Verifies credentials the SC has just pasted, inside the prompt that asked for them. The prompt is the surface; this covers the check.",
        "src/features/project-creation/services/aiBundle/thirdPartyToolingSettingListener.ts:102": "Runs when a VS Code setting changes, so there is no screen and no agent — a notification is the only surface available.",
        "src/features/projects-dashboard/handlers/dashboardHandlers.ts:707": "The Projects list's copy of the incognito-browser wait. Same reason as openUrlHandlers:74, and the two should become one call.",
        "src/features/projects-dashboard/services/settingsTransferService.ts:247": "A progress bar with no work in it: it sleeps for TIMEOUTS.UI.NOTIFICATION so the export confirmation dismisses itself. R6 rules out timed toasts for operation endings, and this is one wearing a progress bar.",
        "src/features/updates/commands/checkUpdates.ts:58": "The update check, from the palette and from activation. Updates narrate themselves through their own notifications end to end.",
        "src/features/updates/commands/updateExecutor.ts:146": "Template sync during an update run. The update flow is a sequence of notifications by design — the SC is not on a screen while it happens.",
        "src/features/updates/commands/updateExecutor.ts:246": "Component update during the same run, and part of the same sequence.",
        "src/features/updates/commands/updateExecutor.ts:342": "Adobe MCP update during the same run, and part of the same sequence.",
        "src/features/updates/services/extensionUpdater.ts:21": "Installs the new VSIX and reloads the window. Nothing of ours survives the reload, so a surface owned by our own state is the wrong place for it."
    },

    "blockingDialogs": {
        "src/commands/ResetAiOnboardingCommand.ts:31": "Gates: reset the AI onboarding surface? Destructive and asked once, before anything runs.",
        "src/commands/ResetAllCommand.ts:28": "Gates: delete all Demo Builder data and state? The most destructive thing the extension offers.",
        "src/commands/manageSiteAccess.ts:303": "Gates: remove this person as a configuration admin? Takes away someone else's access.",
        "src/commands/migrateStorefrontNames.ts:139": "Gates: rename these storefronts? The detail lists exactly which ones, so the SC answers with the list in front of them.",
        "src/core/base/baseCommand.ts:246": "The generic confirm every command asks through. It gates whatever its caller is about to do; the callers are where the decision is named.",
        "src/features/ai/server/agentOperationNotifier.ts:163": "Gates: let the agent run this tool? A cloud write an agent proposes is confirmed by a person before it happens.",
        "src/features/eds/commands/cleanupDaLiveSites.ts:168": "Gates: delete these DA.live sites? Named in the detail, and the content does not come back.",
        "src/features/eds/commands/manageGitHubRepos.ts:168": "Gates: delete these GitHub repositories? Named in the detail, and they do not come back.",
        "src/features/eds/handlers/storefrontSetup/storefrontSetupHandlers.ts:96": "Gates: cancel setup, knowing the GitHub repo and DA.live content created so far will be deleted? The cost of stopping is not obvious without asking.",
        "src/features/eds/services/reset/edsResetUI.ts:412": "Gates: reset this storefront to the template and re-copy demo content? Hand edits are lost.",
        "src/features/eds/services/reset/edsResetUI.ts:647": "Gates: also reset the Commerce backend, which takes several minutes and is not needed for a storefront reset? A separate, costlier choice inside the same flow.",
        "src/features/lifecycle/commands/syncStorefront.ts:222": "Gates: the SC has resolved the merge conflicts and sync can finish. Cancel undoes the pull, so the answer decides between two different end states.",
        "src/features/lifecycle/services/projectResetService.ts:389": "Gates: delete every component and reinstall from scratch? Configuration survives, the components do not.",
        "src/features/updates/commands/templateConflictPrompt.ts:47": "Gates: which side wins where an update and the SC's own edits touched the same file? The update cannot proceed until someone chooses."
    }
}
