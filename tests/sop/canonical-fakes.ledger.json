{
  "logger": [],
  "_castCeilings_what": "A literal cast to a real type is a fake the compiler has been told not to check. Every one of these types ALREADY has a builder in tests/helpers, so the target for each is ZERO and the number may only fall. Seeded 2026-08-31 from a measurement of 1,735 literal-casts across the test tree; these nine are the ones with a builder standing right next to them.",
  "castCeilings": {
    "Project": 6,
    "HandlerContext": 1
  },
  "_castCeilings_Project_floor": "Project reached its FLOOR on 2026-09-03: all 134 sites were read and 128 converted. The 6 that remain are the must-stay list in .rptc/backlog/2026-08-28-shared-test-builders.md: three are this suite's own docblock and CONTROL strings, and three are fixtures whose subject branches on a key the builder supplies (edsResetUI-sampleData: adobe absent; executor-meshStatePopulation: asserts componentConfigs undefined; projectFileLoader-orphanConfigs: the `!configs` early return). A new literal cast to Project is still a regression; lowering below 6 means one of those six changed its reason.",
  "_logger_closed": "CLOSED 2026-09-01. Seeded at 296 hand-rolled logger fakes; the last four were converted the same day (two EDS service suites, one rate-limiting suite whose fake is read inside a jest.mock factory, and one handler testUtils). The list stays as an EMPTY ARRAY rather than being deleted because the two tests below read it: one bans a new hand-rolled fake outright, the other fails on a stale row. With the array empty the first is a flat ban.",
  "castBans": [
    "Logger",
    "ExtensionContext",
    "StateManager",
    "HandlerContext['stateManager']",
    "CommandExecutor",
    "vscode.ExtensionContext",
    "Partial<Project>"
  ],
  "_castBans_what": "Families whose OBJECT-LITERAL casts reached zero. `vscode.ExtensionContext` closed 2026-09-03 (six sites, all onto createMockExtensionContext / createStatefulGlobalState); the earlier `ExtensionContext` ban did not cover the qualified spelling, so both are listed. `Partial<Project>` closed 2026-09-03: 32 of its 38 casts were on literals already handed to createMockProject and compiled without them; the six that did not were two component instances missing required fields and four accessor tests now on the builder. Read the scope precisely: the check is brace-anchored (`} as T`), so it bans a hand-rolled LITERAL standing in for T — the thing a builder replaces. It does NOT ban every `as T`. Measured by AST on 2026-09-01, casts of other expressions to these types remain: Logger 98, StateManager 63, CommandExecutor 9, ExtensionContext 0. Those are a different defect with a different fix (an argument cast is a silenced type error, not a missing builder) and are tracked by `astTotals` below, not by these bans. Entries are never removed.",
  "astTotals": {
    "_what": "Every `as T` anywhere in tests/, counted on the SYNTAX TREE by scripts/codemod/survey-casts.mjs — not the brace-anchored regex the ceilings use. Recorded 2026-09-01 because the two numbers differ a lot and reporting the smaller one as if it were the whole picture overstated what the bans achieved.",
    "_measured": "2026-09-01",
    "Logger": 98,
    "StateManager": 63,
    "CommandExecutor": 9,
    "ExtensionContext": 0,
    "HandlerContext": 100
  }
}
