{
  "_what": "ADR-015 enforcement ledger. Every entry is a KNOWN pre-ruling violation with its reason. The test fails on (a) any violation NOT listed here, (b) any listed entry that no longer violates (the ledger only shrinks), (c) any entry without a reason. Cleaning a file = fixing it AND deleting its row.",
  "fetchBoundary": {},
  "constructionBoundary": {
    "src/core/communication/webviewCommunicationManager.ts": "D-4 (measured 2026-08-28): constructs WebviewCommunicationManager itself, and 2 test suite(s) must module-mock those parts to test around it. That IS the mock wall this program removes — hand them in. Ranked by suite count.",
    "src/core/shell/commandExecutor.ts": "D-4 (measured 2026-08-28): constructs CommandQueue, CommandResultCache, CommandSequencer, EnvironmentSetup itself, and 13 test suite(s) must module-mock those parts to test around it. That IS the mock wall this program removes — hand them in. Ranked by suite count.",
    "src/core/shell/fileWatcher.ts": "D-4 (measured 2026-08-28): constructs PollingService itself, and 12 test suite(s) must module-mock those parts to test around it. That IS the mock wall this program removes — hand them in. Ranked by suite count.",
    "src/core/state/stateManager.ts": "D-4 (measured 2026-08-28): constructs ExecutionLock, ProjectConfigWriter, ProjectDirectoryScanner, ProjectFileLoader itself. ADR-015 allows construction only in extension.ts and create...Deps files, so this violates the rule as written — but it forces NO module mock in any suite today, so the cost is currently zero. Lowest priority; convert when the file is touched.",
    "src/core/ui/utils/WebviewClient.ts": "D-4 (measured 2026-08-28): constructs WebviewClient itself, and 59 test suite(s) must module-mock those parts to test around it. That IS the mock wall this program removes — hand them in. Ranked by suite count.",
    "src/features/app-builder/services/appManagementInstaller.ts": "D-4 (measured 2026-08-28): constructs AppManagementClient itself, and 1 test suite(s) must module-mock those parts to test around it. That IS the mock wall this program removes — hand them in. Ranked by suite count.",
    "src/features/app-builder/services/appManagementUninstaller.ts": "D-4 (measured 2026-08-28): constructs AppManagementClient itself, and 1 test suite(s) must module-mock those parts to test around it. That IS the mock wall this program removes — hand them in. Ranked by suite count.",
    "src/features/authentication/services/adobeSDKClient.ts": "D-4 (measured 2026-08-28): constructs TokenManager itself, and 1 test suite(s) must module-mock those parts to test around it. That IS the mock wall this program removes — hand them in. Ranked by suite count.",
    "src/features/authentication/services/authenticationService.ts": "D-4 (measured 2026-08-28): constructs AdobeSDKClient, AuthCacheManager, OrganizationValidator, TokenManager itself, and 6 test suite(s) must module-mock those parts to test around it. That IS the mock wall this program removes — hand them in. Ranked by suite count.",
    "src/features/eds/services/authoringExperienceFlip.ts": "D-2 group 1: constructs its own GitHubTokenService instead of asking edsServiceCache.getGitHubServices, whose instance carries a token-VALIDATION cache. A fresh instance re-validates against GitHub. Route it through the builder, or take the token service as a parameter where no handler context is reachable.",
    "src/features/eds/services/configService/repairSiteConfigForProject.ts": "RATIFIED 2026-08-28 (D-2): builds an EDS client that takes a per-project TOKEN PROVIDER, so instances differ by call site and cannot be shared. Constructing it here with the auth this call needs is correct — not debt. Do not converge.",
    "src/features/eds/services/configSyncService.ts": "D-2 group 1: constructs its own GitHubTokenService instead of asking edsServiceCache.getGitHubServices, whose instance carries a token-VALIDATION cache. A fresh instance re-validates against GitHub. Route it through the builder, or take the token service as a parameter where no handler context is reachable.",
    "src/features/eds/services/daLive/daLiveConfigService.ts": "D-4 (measured 2026-08-28): constructs DaLiveApiClient itself. ADR-015 allows construction only in extension.ts and create...Deps files, so this violates the rule as written — but it forces NO module mock in any suite today, so the cost is currently zero. Lowest priority; convert when the file is touched.",
    "src/features/eds/services/daLive/daLiveContentOperations.ts": "RATIFIED 2026-08-28 (D-2): builds an EDS client that takes a per-project TOKEN PROVIDER, so instances differ by call site and cannot be shared. Constructing it here with the auth this call needs is correct — not debt. Do not converge.",
    "src/features/eds/services/daLive/daLiveOrgOperations.ts": "D-4 (measured 2026-08-28): constructs DaLiveApiClient, DaLiveAuthError, DaLiveError itself. ADR-015 allows construction only in extension.ts and create...Deps files, so this violates the rule as written — but it forces NO module mock in any suite today, so the cost is currently zero. Lowest priority; convert when the file is touched.",
    "src/features/eds/services/github/githubRepoOperations.ts": "D-4 (measured 2026-08-28): constructs PollingService itself, and 12 test suite(s) must module-mock those parts to test around it. That IS the mock wall this program removes — hand them in. Ranked by suite count.",
    "src/features/eds/services/pdp/publishKeyRegistrar.ts": "RATIFIED 2026-08-28 (D-2): builds an EDS client that takes a per-project TOKEN PROVIDER, so instances differ by call site and cannot be shared. Constructing it here with the auth this call needs is correct — not debt. Do not converge.",
    "src/features/eds/services/refreshBlockLibraryHeadless.ts": "RATIFIED 2026-08-28 (D-2): builds an EDS client that takes a per-project TOKEN PROVIDER, so instances differ by call site and cannot be shared. Constructing it here with the auth this call needs is correct — not debt. Do not converge.",
    "src/features/eds/services/reset/edsResetConfigStep.ts": "RATIFIED 2026-08-28 (D-2): builds an EDS client that takes a per-project TOKEN PROVIDER, so instances differ by call site and cannot be shared. Constructing it here with the auth this call needs is correct — not debt. Do not converge.",
    "src/features/eds/services/reset/edsResetService.ts": "RATIFIED 2026-08-28 (D-2): builds an EDS client that takes a per-project TOKEN PROVIDER, so instances differ by call site and cannot be shared. Constructing it here with the auth this call needs is correct — not debt. Do not converge.",
    "src/features/eds/services/reset/edsResetUI.ts": "D-4 (measured 2026-08-28): constructs GitHubAppService itself. ADR-015 allows construction only in extension.ts and create...Deps files, so this violates the rule as written — but it forces NO module mock in any suite today, so the cost is currently zero. Lowest priority; convert when the file is touched.",
    "src/features/eds/services/storefront/storefrontNameMigrationForProject.ts": "RATIFIED 2026-08-28 (D-2): builds an EDS client that takes a per-project TOKEN PROVIDER, so instances differ by call site and cannot be shared. Constructing it here with the auth this call needs is correct — not debt. Do not converge.",
    "src/features/eds/services/storefront/storefrontRepublishService.ts": "RATIFIED 2026-08-28 (D-2): builds an EDS client that takes a per-project TOKEN PROVIDER, so instances differ by call site and cannot be shared. Constructing it here with the auth this call needs is correct — not debt. Do not converge.",
    "src/features/lifecycle/services/projectResetService.ts": "D-4 (measured 2026-08-28): constructs ComponentRegistryManager itself, and 17 test suite(s) must module-mock those parts to test around it. That IS the mock wall this program removes — hand them in. Ranked by suite count.",
    "src/features/prerequisites/services/PrerequisitesManager.ts": "D-4 (measured 2026-08-28): constructs PrerequisitesCacheManager itself. ADR-015 allows construction only in extension.ts and create...Deps files, so this violates the rule as written — but it forces NO module mock in any suite today, so the cost is currently zero. Lowest priority; convert when the file is touched.",
    "src/features/project-creation/services/catalogPrewarmPhase.ts": "D-2 group 1: constructs its own GitHubTokenService instead of asking edsServiceCache.getGitHubServices, whose instance carries a token-VALIDATION cache. A fresh instance re-validates against GitHub. Route it through the builder, or take the token service as a parameter where no handler context is reachable.",
    "src/features/project-creation/services/componentInstallationOrchestrator.ts": "D-4 (measured 2026-08-28): constructs ComponentManager itself, and 4 test suite(s) must module-mock those parts to test around it. That IS the mock wall this program removes — hand them in. Ranked by suite count.",
    "src/features/project-creation/services/edsContentSetup.ts": "D-2 group 1: constructs its own GitHubTokenService instead of asking edsServiceCache.getGitHubServices, whose instance carries a token-VALIDATION cache. A fresh instance re-validates against GitHub. Route it through the builder, or take the token service as a parameter where no handler context is reachable.",
    "src/features/projects-dashboard/services/projectDeletionService.ts": "RATIFIED 2026-08-28 (D-2): builds an EDS client that takes a per-project TOKEN PROVIDER, so instances differ by call site and cannot be shared. Constructing it here with the auth this call needs is correct — not debt. Do not converge.",
    "src/features/updates/services/componentUpdater.ts": "D-4 (measured 2026-08-28): constructs ComponentRegistryManager itself, and 17 test suite(s) must module-mock those parts to test around it. That IS the mock wall this program removes — hand them in. Ranked by suite count.",
    "src/features/updates/services/templateSyncService.ts": "D-2 group 1: constructs its own GitHubTokenService instead of asking edsServiceCache.getGitHubServices, whose instance carries a token-VALIDATION cache. A fresh instance re-validates against GitHub. Route it through the builder, or take the token service as a parameter where no handler context is reachable.",
    "src/features/updates/services/updateApplyService.ts": "D-4 (measured 2026-08-28): constructs AddonUpdateChecker, AdobeMcpUpdateChecker, ComponentUpdater, ForkSyncService itself, and 5 test suite(s) must module-mock those parts to test around it. That IS the mock wall this program removes — hand them in. Ranked by suite count.",
    "src/features/updates/services/updateCore.ts": "D-2 group 1: constructs its own GitHubTokenService instead of asking edsServiceCache.getGitHubServices, whose instance carries a token-VALIDATION cache. A fresh instance re-validates against GitHub. Route it through the builder, or take the token service as a parameter where no handler context is reachable.",
    "src/features/mesh/services/deployMeshHeadless.ts": "D-4 (measured 2026-08-28): constructs ComponentRegistryManager itself, and 17 test suite(s) must module-mock those parts to test around it. That IS the mock wall this program removes — hand them in. Ranked by suite count."
  },
  "commandBase": {
    "src/commands/commandManager.ts:CommandManager": "the command REGISTRAR, not a command — exempt by role",
    "src/commands/diagnostics.ts:DiagnosticsCommand": "pre-ADR-015: converge to BaseCommand"
  },
  "typesPurity": {
    "src/types/typeGuards.ts": "runtime type guards colocated in types/ by long-standing convention — candidate for ratification",
    "src/types/errors.ts": "pre-ADR-015: runtime import in a types file — adjudicate (move code out, or ratify the exception)",
    "src/types/handlers.ts": "pre-ADR-015: runtime import in a types file — adjudicate (move code out, or ratify the exception)",
    "src/types/shell.ts": "pre-ADR-015: runtime import in a types file — adjudicate (move code out, or ratify the exception)",
    "src/types/state.ts": "pre-ADR-015: runtime import in a types file — adjudicate (move code out, or ratify the exception)",
    "src/types/webview.ts": "pre-ADR-015: runtime import in a types file — adjudicate (move code out, or ratify the exception)"
  },
  "hookRefs": {
    "src/core/ui/hooks/useActivateOnKey.ts": "flagged by the coarse inline-literal detector — adjudicate: legitimate default value vs unstable reference",
    "src/core/ui/hooks/useFocusOnMount.ts": "flagged by the coarse inline-literal detector — adjudicate: legitimate default value vs unstable reference",
    "src/features/components/ui/hooks/useStoreDiscovery.ts": "flagged by the coarse inline-literal detector — adjudicate: legitimate default value vs unstable reference",
    "src/features/dashboard/ui/hooks/useDashboardStatus.ts": "flagged by the coarse inline-literal detector — adjudicate: legitimate default value vs unstable reference",
    "src/features/project-creation/ui/components/ConnectStoreStepContent.tsx": "flagged by the coarse inline-literal detector — adjudicate: legitimate default value vs unstable reference"
  },
  "patternBSendMessageCeiling": 147
}
