{
    "_what": "Enforcement ledger for the EXTENSION HOST dependency rules (webview files are enforced by webview-architecture-rules.test.ts under ADR-017). The keys map to rules that live in different records since the 2026-08-30 split: fetchBoundary and constructionBoundary -> ADR-015; compositionPointLifetime -> ADR-020; commandBase and typesPurity -> the development handbook (no ADR \u2014 neither has a rejected alternative worth recording). Every entry is a KNOWN pre-ruling violation with its reason. The test fails on (a) any violation NOT listed here, (b) any listed entry that no longer violates (the ledger only shrinks), (c) any entry without a reason. Cleaning a file = fixing it AND deleting its row.",
    "fetchBoundary": {},
    "constructionBoundary": {},
    "commandBase": {
        "src/commands/commandManager.ts:CommandManager": "the command REGISTRAR, not a command \u2014 exempt by role. RATIFIED as permanent: it builds and registers the 25 commands, so it cannot itself be one of them. Reviewed 2026-08-31 when the other row in this category was cleared; this is the only member left and it is not debt."
    },
    "typesPurity": {
        "src/types/typeGuards.ts": "RATIFIED by the owner 2026-08-31. Runtime type guards colocated with the types they narrow, and genuinely runtime: needs COMPONENT_IDS, getMeshAppBuilderComponent, deriveAccsAdminUrl, lookupComponentConfigValue and two endpoint constants as VALUES \u2014 measured by converting every import in the file to type-only and reading tsc. A type guard is the one kind of runtime code that belongs beside its types; separating it would make the guards harder to find and they are imported very widely. PERMANENT EXCEPTION, not debt."
    },
    "patternBSendMessageCeiling": 142,
    "compositionPointLifetime": {},
    "featureBarrels": {},
    "_featureBarrels_note": "ADR-022 forbids NEW feature barrels; these five predate it. The ledger only shrinks, so retiring one means deleting its row in the same commit. Counts are importers of the bare `@/features/<name>` specifier, measured 2026-08-30.",
    "layerDirection": {
        "src/core/di/serviceLocator.ts": "RATIFIED by the owner 2026-08-31. A service locator has to name what it locates. Type-only imports of AuthenticationService and SidebarProvider, so no runtime coupling and no cycle. Measured before ratifying: extracting interfaces does not work (both are CLASSES, and AuthenticationService has 44 public methods across 855 lines, so the copy would drift), and converting callers does not either (all 48 are handlers, commands or MCP tools, which the rule explicitly permits to fetch). The alternative considered and rejected was a typed-token registry, which removes the naming but rewrites the DI shape and 50+ call sites. PERMANENT EXCEPTION, not debt."
    },
    "_layerDirection_note": "Direction rule: nothing under src/core/ imports @/features or @/commands. Ratified 2026-08-30 after the rule was found stated as law in src/core/CLAUDE.md, present in NO handbook/ADR/convention, enforced by nothing, and violated seven times. Webview files are out of scope (isWebview), so core/ui/ is excluded. The set may only shrink: clearing a file means fixing it AND deleting its row.",
    "reExportIndex": {
        "src/types/index.ts": "PURE: 11 re-export line(s); ~172 importer(s) via the barrel path. Removing it means repointing those importers to the modules that declare the symbols.",
        "src/core/ui/components/ui/index.ts": "MIXED: 13 re-export line(s), 6 own declaration(s); ~2 importer(s) via the barrel path. Its own declarations must move to a named module FIRST \u2014 a file that both declares and re-exports is holding public and private code together.",
        "src/core/ui/components/forms/index.ts": "MIXED: 8 re-export line(s), 4 own declaration(s); ~9 importer(s) via the barrel path. Its own declarations must move to a named module FIRST \u2014 a file that both declares and re-exports is holding public and private code together.",
        "src/core/ui/components/layout/index.ts": "MIXED: 22 re-export line(s), 11 own declaration(s); ~6 importer(s) via the barrel path. Its own declarations must move to a named module FIRST \u2014 a file that both declares and re-exports is holding public and private code together.",
        "src/core/ui/components/navigation/index.ts": "MIXED: 6 re-export line(s), 3 own declaration(s); ~1 importer(s) via the barrel path. Its own declarations must move to a named module FIRST \u2014 a file that both declares and re-exports is holding public and private code together.",
        "src/core/ui/components/feedback/index.ts": "MIXED: 15 re-export line(s), 7 own declaration(s); ~7 importer(s) via the barrel path. Its own declarations must move to a named module FIRST \u2014 a file that both declares and re-exports is holding public and private code together.",
        "src/core/ui/components/integrations/index.ts": "MIXED: 3 re-export line(s), 1 own declaration(s); ~6 importer(s) via the barrel path. Its own declarations must move to a named module FIRST \u2014 a file that both declares and re-exports is holding public and private code together.",
        "src/core/ui/components/selection/index.ts": "PURE: 2 re-export line(s); ~7 importer(s) via the barrel path. Removing it means repointing those importers to the modules that declare the symbols.",
        "src/core/ui/hooks/index.ts": "MIXED: 31 re-export line(s), 8 own declaration(s); ~15 importer(s) via the barrel path. Its own declarations must move to a named module FIRST \u2014 a file that both declares and re-exports is holding public and private code together.",
        "src/core/di/index.ts": "PURE: 1 re-export line(s); ~85 importer(s) via the barrel path. Removing it means repointing those importers to the modules that declare the symbols.",
        "src/core/cache/index.ts": "PURE: 1 re-export line(s); ~3 importer(s) via the barrel path. Removing it means repointing those importers to the modules that declare the symbols.",
        "src/core/shell/index.ts": "MIXED: 12 re-export line(s), 3 own declaration(s); ~103 importer(s) via the barrel path. Its own declarations must move to a named module FIRST \u2014 a file that both declares and re-exports is holding public and private code together.",
        "src/core/utils/index.ts": "MIXED: 18 re-export line(s), 4 own declaration(s); ~32 importer(s) via the barrel path. Its own declarations must move to a named module FIRST \u2014 a file that both declares and re-exports is holding public and private code together.",
        "src/core/utils/progressUnifier/index.ts": "PURE: 2 re-export line(s); ~9 importer(s) via the barrel path. Removing it means repointing those importers to the modules that declare the symbols.",
        "src/core/state/index.ts": "MIXED: 12 re-export line(s), 4 own declaration(s); ~58 importer(s) via the barrel path. Its own declarations must move to a named module FIRST \u2014 a file that both declares and re-exports is holding public and private code together.",
        "src/core/communication/index.ts": "PURE: 1 re-export line(s); ~9 importer(s) via the barrel path. Removing it means repointing those importers to the modules that declare the symbols.",
        "src/core/vscode/index.ts": "PURE: 2 re-export line(s); ~1 importer(s) via the barrel path. Removing it means repointing those importers to the modules that declare the symbols.",
        "src/core/handlers/index.ts": "PURE: 3 re-export line(s); ~13 importer(s) via the barrel path. Removing it means repointing those importers to the modules that declare the symbols.",
        "src/core/logging/index.ts": "MIXED: 5 re-export line(s), 2 own declaration(s); ~89 importer(s) via the barrel path. Its own declarations must move to a named module FIRST \u2014 a file that both declares and re-exports is holding public and private code together. *** RETIRING THIS ONE BREAKS THE TEST SUITE UNLESS tests/setup/node.ts MOVES WITH IT. *** The global getLogger mock there targets '@/core/logging' \u2014 this barrel. 53 source files import it and would all switch to '@/core/logging/debugLogger' on conversion, at which point the setup mock intercepts nothing and getLogger() throws across the suite. Proven 2026-08-31 on the ONE file that already imports the deep module directly (prerequisitesCacheManager.ts): its tests cannot use the global and carry their own mock, and removing that mock fails 14 tests with 'Logger not initialized'. Convert the setup mock to the deep module IN THE SAME COMMIT, and expect the ten suites under tests/core/logging that test the logger itself to need jest.unmock \u2014 measured, not guessed.",
        "src/core/base/index.ts": "PURE: 2 re-export line(s); ~32 importer(s) via the barrel path. Removing it means repointing those importers to the modules that declare the symbols.",
        "src/core/validation/index.ts": "PURE: 7 re-export line(s); ~54 importer(s) via the barrel path. Removing it means repointing those importers to the modules that declare the symbols.",
        "src/core/validation/validators/index.ts": "PURE: 4 re-export line(s); ~1 importer(s) via the barrel path. Removing it means repointing those importers to the modules that declare the symbols.",
        "src/features/mesh/handlers/index.ts": "PURE: 1 re-export line(s); ~3 importer(s) via the barrel path. Removing it means repointing those importers to the modules that declare the symbols.",
        "src/features/sidebar/ui/views/index.ts": "MIXED: 2 re-export line(s), 1 own declaration(s); ~1 importer(s) via the barrel path. Its own declarations must move to a named module FIRST \u2014 a file that both declares and re-exports is holding public and private code together.",
        "src/features/data-installer/handlers/index.ts": "MIXED: 4 re-export line(s), 1 own declaration(s); ~7 importer(s) via the barrel path. Its own declarations must move to a named module FIRST \u2014 a file that both declares and re-exports is holding public and private code together.",
        "src/features/eds/ui/components/index.ts": "MIXED: 7 re-export line(s), 3 own declaration(s); ~3 importer(s) via the barrel path. Its own declarations must move to a named module FIRST \u2014 a file that both declares and re-exports is holding public and private code together.",
        "src/features/eds/handlers/index.ts": "PURE: 8 re-export line(s); ~3 importer(s) via the barrel path. Removing it means repointing those importers to the modules that declare the symbols.",
        "src/features/prerequisites/ui/steps/hooks/index.ts": "MIXED: 5 re-export line(s), 1 own declaration(s); ~1 importer(s) via the barrel path. Its own declarations must move to a named module FIRST \u2014 a file that both declares and re-exports is holding public and private code together.",
        "src/features/prerequisites/handlers/index.ts": "PURE: 4 re-export line(s); ~2 importer(s) via the barrel path. Removing it means repointing those importers to the modules that declare the symbols.",
        "src/features/prerequisites/services/installation/index.ts": "MIXED: 2 re-export line(s), 1 own declaration(s); ~1 importer(s) via the barrel path. Its own declarations must move to a named module FIRST \u2014 a file that both declares and re-exports is holding public and private code together.",
        "src/features/prerequisites/services/versioning/index.ts": "MIXED: 5 re-export line(s), 1 own declaration(s); ~3 importer(s) via the barrel path. Its own declarations must move to a named module FIRST \u2014 a file that both declares and re-exports is holding public and private code together.",
        "src/features/dashboard/ui/hooks/index.ts": "MIXED: 7 re-export line(s), 3 own declaration(s); ~0 importer(s) via the barrel path. Its own declarations must move to a named module FIRST \u2014 a file that both declares and re-exports is holding public and private code together.",
        "src/features/dashboard/handlers/index.ts": "PURE: 1 re-export line(s); ~2 importer(s) via the barrel path. Removing it means repointing those importers to the modules that declare the symbols.",
        "src/features/dashboard/services/onOpenChecks/index.ts": "MIXED: 7 re-export line(s), 1 own declaration(s); ~11 importer(s) via the barrel path. Its own declarations must move to a named module FIRST \u2014 a file that both declares and re-exports is holding public and private code together.",
        "src/features/project-creation/ui/wizard/hooks/index.ts": "PURE: 4 re-export line(s); ~1 importer(s) via the barrel path. Removing it means repointing those importers to the modules that declare the symbols.",
        "src/features/project-creation/ui/components/index.ts": "PURE: 1 re-export line(s); ~0 importer(s) via the barrel path. Removing it means repointing those importers to the modules that declare the symbols.",
        "src/features/project-creation/ui/components/integration-flow/index.ts": "MIXED: 9 re-export line(s), 4 own declaration(s); ~6 importer(s) via the barrel path. Its own declarations must move to a named module FIRST \u2014 a file that both declares and re-exports is holding public and private code together.",
        "src/features/project-creation/handlers/index.ts": "PURE: 6 re-export line(s); ~3 importer(s) via the barrel path. Removing it means repointing those importers to the modules that declare the symbols.",
        "src/features/project-creation/helpers/index.ts": "MIXED: 5 re-export line(s), 2 own declaration(s); ~9 importer(s) via the barrel path. Its own declarations must move to a named module FIRST \u2014 a file that both declares and re-exports is holding public and private code together.",
        "src/features/project-creation/services/index.ts": "PURE: 7 re-export line(s); ~10 importer(s) via the barrel path. Removing it means repointing those importers to the modules that declare the symbols.",
        "src/features/projects-dashboard/ui/components/index.ts": "MIXED: 8 re-export line(s), 4 own declaration(s); ~0 importer(s) via the barrel path. Its own declarations must move to a named module FIRST \u2014 a file that both declares and re-exports is holding public and private code together.",
        "src/features/projects-dashboard/handlers/index.ts": "PURE: 2 re-export line(s); ~3 importer(s) via the barrel path. Removing it means repointing those importers to the modules that declare the symbols.",
        "src/features/projects-dashboard/services/index.ts": "MIXED: 5 re-export line(s), 1 own declaration(s); ~2 importer(s) via the barrel path. Its own declarations must move to a named module FIRST \u2014 a file that both declares and re-exports is holding public and private code together."
    }
}
