{
    "_what": "One row per CREATE-SHAPED capability on the agent surface. Each row either NAMES the tool that undoes it, or carries a `reason` saying why no reversal exists. This is CLAUDE.md's first never-compromise property written down: 'New capabilities ship with their reversal - create/delete, deploy/undeploy, install/uninstall - or they state plainly why reversal is impossible.' Enforced by tests/sop/reversibility-ledger.test.ts.",
    "_rules": [
        "A create-shaped tool with no row fails the build. Adding the tool means answering the question.",
        "`reverses` names a tool that EXISTS. A row pointing at a tool nobody registered is stale and fails.",
        "`reason` is for a capability that genuinely cannot be undone, or whose reversal is a product decision nobody has taken. It is not a way to skip the question - the text is read by a person.",
        "A `reason` row is a DEBT, not a resolution. The set of them should shrink, and the enforcer pins it so it cannot grow silently.",
        "The reversal need not share a verb stem. `deploy_mesh` is undone by `delete_mesh`, `publish_page` by `delete_page`. A naming rule was measured and REJECTED - it wrongly flags at least three - see PL-55's design section."
    ],
    "_measured": "2026-09-10: 109 tools across both registration forms, read via tests/sop/tool.testUtils.ts (`src/features/ai/server/*.ts`, `src/mcp-server.ts`, `src/mcp/*.ts`). 15 create-shaped. An earlier hand-rolled walk read only one directory and found 100 - the shared helper exists because that mistake has now been made twice.",
    "openReasonCeiling": 3,
    "entries": [
        {
            "tool": "add_integration",
            "reverses": "remove_integration"
        },
        {
            "tool": "create_adobe_project",
            "reverses": "delete_adobe_project"
        },
        {
            "tool": "create_github_repo",
            "reverses": "delete_github_repo"
        },
        {
            "tool": "create_project",
            "reverses": "delete_project"
        },
        {
            "tool": "start_demo",
            "reverses": "stop_demo"
        },
        {
            "tool": "deploy_mesh",
            "reverses": "delete_mesh",
            "note": "Different verb stem, deliberately: the mesh is deleted rather than undeployed. This row is why the enforcer pairs by ledger and not by name."
        },
        {
            "tool": "deploy_integration",
            "reverses": "remove_integration",
            "note": "remove_integration undeploys it remotely, then cleans up that integration's files and keyed state. AB-7 is an open defect in exactly this reversal - it reported success while leaving deployed code running - which is what an unenforced principle looks like in production."
        },
        {
            "tool": "install_integration",
            "reverses": "remove_integration",
            "note": "install_integration re-runs the Commerce install/associate pass for an already-deployed integration; remove_integration uninstalls it from Commerce before undeploying."
        },
        {
            "tool": "publish_page",
            "reverses": "delete_page",
            "note": "delete_page unpublishes AND deletes the source, so it is a WIDER reversal than publish_page's exact inverse - there is no unpublish-but-keep-source tool. Recorded because the published state can be undone; narrowing it is a product question, not a defect."
        },
        {
            "tool": "install_prerequisite",
            "reason": "DECIDED 2026-09-11 (owner): uninstall should NOT exist. Node, the aio CLI and its plugins are machine-level tools an SC almost certainly uses outside this extension; removing them because a demo ended is a worse outcome than the asymmetry. This is a stated decision, not an open question."
        },
        {
            "tool": "create_adobe_workspace",
            "reverses": "delete_adobe_workspace"
        },
        {
            "tool": "add_console_apis",
            "reverses": "set_console_apis",
            "note": "Corrected 2026-09-11: the first pass recorded this as having no reversal. set_console_apis sets the subscriptions to EXACTLY a list -- 'anything currently subscribed and not listed is REMOVED, pass an empty array to clear the extras' -- so it is the un-add. The handbook already described it as 'a delete wearing a setter's name'. Recording a gap without checking whether the capability already existed is how a ledger becomes fiction."
        },
        {
            "tool": "connect_dalive",
            "reason": "A sign-in, not a created resource - nothing is provisioned that could be deleted, so reversibility does not apply. DECIDED 2026-09-11 (owner): whether an agent should be able to sign you OUT is a question about agent power rather than about undo, and is out of scope for this ledger."
        },
        {
            "tool": "start_datapack_export",
            "reason": "Writes into the SHARED datapack catalog, so the reversal is not merely a delete - it would have to consider other consumers. DECIDED 2026-09-11 (owner): design it now. Note the boundary - the catalog is owned by the data-installer service, so the design may conclude the reversal does not live in this repo."
        },
        {
            "tool": "start_datapack_import",
            "reverses": "reset_datapack",
            "note": "Corrected 2026-09-11, same mistake: reset_datapack removes a datapack's data from the Commerce instance so the project can be reused, which is precisely what the import put there. The first pass called it 'adjacent' without reading its description."
        }
    ]
}
