# AI Feature

## Purpose

This feature verifies the AI context files generated at project creation and provides the **AI inventory backend** that powers the Configure screen's **AI Configuration tab** (`AiConfigurationTab.tsx`) per-skill/per-MCP introspection.

The AI context files themselves are generated by three services in `src/features/project-creation/services/` and exposed to AI agents via the standalone MCP server (`src/mcp-server.ts`).

The harness is **Claude Code (CLI)** — users open a generated project with `claude` from the project directory. The Demo Builder MCP server is discoverable via the global `~/.claude/.mcp.json` entry written on extension activation, and via the project-local `.claude/mcp.json` written during project creation.

---

## Files

| File | Purpose |
|------|---------|
| `aiSetupVerifier.ts` | File-presence checks + `gatherInventory` orchestrator; returns `AiVerificationResult` |
| `skillInspector.ts` | Walks `.claude/skills/`, parses YAML frontmatter, classifies skills (`demo-builder` / `adobe` / `unknown`) |
| `mcpInspector.ts` | Spawns each declared MCP server via `@modelcontextprotocol/sdk` stdio client, returns tool inventory with TTL cache |
| `sessionMcpDetector.ts` | Reads `~/.claude.json` + `~/.claude/mcp-needs-auth-cache.json` to enumerate session-level Adobe MCPs |
| `index.ts` | Public API exports |

---

## Key APIs

### `verifyAiSetup(projectPath, extensionDistPath): Promise<AiVerificationResult>`

Runs four file-presence checks in parallel with `gatherInventory()`:

| Check | What it looks for |
|-------|-------------------|
| `claudeMd` | `.claude/CLAUDE.md` exists and is non-empty |
| `mcpConfig` | `.claude/mcp.json` exists and is valid JSON with a `mcpServers` key |
| `mcpBinary` | `{extensionDistPath}/mcp-server.js` exists |
| `skillFiles` | At least one `.md` file exists in `.claude/skills/` |

Returns `AiVerificationResult` with `{ status, checks, inventory }`. The aggregate `status` reflects the checks (`error > warning > ok`); the `inventory` field carries the three inspector outputs (see below).

### `gatherInventory(projectPath): Promise<AiInventory>`

Runs the three inspectors via `Promise.allSettled` so a failing inspector degrades to an empty list rather than failing the whole verification. Each failed inspector also populates a matching `*Error` field (e.g., `skillsError`, `mcpsError`, `sessionMcpsError`) so UIs can distinguish "no items" from "introspection failed."

### `inspectSkills(projectPath): Promise<SkillInventoryEntry[]>`

Walks `<project>/.claude/skills/` and returns one entry per `.md` file with `{ name, description, path, source }`. Classification: top-level Demo Builder lifecycle skills → `'demo-builder'`; nested in an Adobe bundle subdir → `'adobe'`; other top-level → `'unknown'`.

### `inspectAllServers(projectPath): Promise<McpInventoryEntry[]>` + `clearMcpCache(serverId?)`

Reads `<project>/.claude/mcp.json`, spawns each declared server via the SDK's `Client` + `StdioClientTransport`, paginates through `tools/list`, returns per-server `{ id, status, tools?, error? }`. Module-level `Map` cache with 5-minute TTL (±10% jitter via `getCacheTTLWithJitter`). Per-server 15s overall budget (`MCP_INSPECT_TIMEOUT_MS`). Spawned children inherit only the SDK's safe env allowlist (PATH/HOME/USER/SHELL/TERM/LANG/TMPDIR) — extension host secrets do not leak. `clearMcpCache()` clears every entry; `clearMcpCache(id)` clears a single entry.

### `detectSessionMcps(): Promise<SessionMcpEntry[]>`

Returns one entry per name in `~/.claude.json::claudeAiMcpEverConnected`, with `needsAuth` derived by cross-referencing `~/.claude/mcp-needs-auth-cache.json`. **Both files are undocumented Claude Code internal state**; `needsAuth: false` means "not currently flagged for re-auth" — not a hard "authenticated" guarantee.

---

## Architecture

The AI context files live in the project directory, not in this feature:

```
<projectPath>/
├── AGENTS.md              ← generated by aiContextWriter.ts (universal AI context)
├── CLAUDE.md              ← one-line pointer to AGENTS.md
├── .claude/
│   ├── CLAUDE.md          ← one-line pointer to AGENTS.md
│   ├── mcp.json           ← generated by mcpConfigWriter.ts
│   ├── settings.json      ← generated by mcpConfigWriter.ts (PostToolUse hook)
│   └── skills/
│       ├── add-component.md
│       ├── sync-changes.md
│       └── update-credentials.md   ← generated by skillsWriter.ts
└── .mcp.json              ← generated by mcpConfigWriter.ts (Claude Code project scope)
```

The three writers live in `src/features/project-creation/services/`:
- `aiContextWriter.ts` — generates `AGENTS.md` and the `CLAUDE.md` pointers
- `skillsWriter.ts` — writes `.claude/skills/*.md` guides
- `mcpConfigWriter.ts` — generates `.mcp.json`, `.claude/mcp.json`, `.claude/settings.json`

The orchestrator is `projectFinalizationService.generateAIContextFiles()`, called as Phase 6 of project creation and from `configureHandlers.ts` for on-demand regeneration.

The **MCP server** is compiled from `src/mcp-server.ts` (not from inside this feature directory) to `dist/mcp-server.js`. It runs as a standalone stdio process — no VS Code dependency — and exposes seven tools to AI agents:

| Tool | Description |
|------|-------------|
| `list_projects` | Lists all Demo Builder projects |
| `get_project` | Reads `.demo-builder.json` |
| `get_component_config` | Reads `.demo-builder.json` or a `.env` file |
| `update_project_config` | Writes `.demo-builder.json` or a `.env` file |
| `sync_storefront` | Git add / commit / push in the storefront directory |
| `list_blocks` | Lists block directory names |
| `get_block_source` | Reads all source files for a named block |

---

## Integration Points

- **Configure screen** (`src/features/dashboard/ui/tabs/AiConfigurationTab.tsx`): renders the full inventory payload alongside the file-presence checks. Drill-down sections expose per-skill metadata, per-MCP-server tool lists, and session-MCP entries. Actions: Refresh (calls `inspect-mcp`), Regenerate AI Files (`regenerate-ai-files` then re-verify), Register Global MCP (`register-global-mcp` — only when the global registration state is not `'registered'`).
- **`inspect-mcp` handler** (`configureHandlers.ts`): clears the `mcpInspector` cache (per-server or all) and returns fresh `inspectAllServers` output. Used by the AI Configuration tab for on-demand refresh.
- **`register-global-mcp` handler** (`configureHandlers.ts`): calls `registerGlobalMcp(extensionDistPath)` and updates `globalState[GLOBAL_MCP_REG_STATE_KEY] = 'registered'`. Used by the AI Configuration tab's Register button (explicit user intent — bypasses the activation-time consent prompt).
- **`verify-ai-setup` handler** (`configureHandlers.ts`): returns `AiVerificationResult` extended with `globalMcpRegistration` (read from globalState) so the tab can render the fourth Status row without a separate round-trip.
- **`Open in Claude Code` command** (`src/commands/openInClaude.ts`): consults `demoBuilder.ai.harness` and launches either via the URI handler (`vscode://anthropic.claude-code/open`) or via a terminal running `claude` in the project cwd. Dispatched from the dashboard tile and the project-card kebab menu.
- **Project creation** (`projectFinalizationService.ts`): `generateAIContextFiles()` runs all three writers after project setup completes. Also runs as the post-step of `Demo Builder: Check for Updates` when an Adobe MCP package update is applied.
- **Extension activation** (`extension.ts`): consent-gated `ensureGlobalMcpRegistration` upserts the Demo Builder MCP entry into `~/.claude.json` so Claude Code (CLI) can discover it from any project.

---

## Testing

Tests for this feature live in `tests/features/ai/`:
- `aiSetupVerifier.test.ts` — file-presence checks + inventory payload + `gatherInventory` orchestration
- `skillInspector.test.ts` — classification + frontmatter parsing
- `mcpInspector.test.ts` — SDK client integration, pagination, timeouts, cache lifecycle, env allowlist
- `sessionMcpDetector.test.ts` — `~/.claude.json` + needs-auth cache cross-reference
- `mcpServer.test.ts` — standalone MCP server tools

Tests for the three project-creation services live in `tests/features/project-creation/services/`.
