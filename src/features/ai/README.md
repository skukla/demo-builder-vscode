# AI Feature

## Purpose

This feature verifies the AI context files generated at project creation and provides the **AI inventory backend** that powers the standalone **AI surface** (`AiOverviewScreen.tsx`) per-skill/per-MCP introspection.

The AI context files themselves are generated by three services in `src/features/project-creation/services/` and exposed to AI agents via the in-extension MCP server (`server/inExtensionMcpServer.ts`). **For the full MCP architecture and tool catalog, see `docs/systems/mcp-server.md`.**

The harness is **Claude Code (CLI)** — users open a generated project with `claude` from the project directory. The Demo Builder MCP server is discoverable via the global `~/.claude/.mcp.json` entry written on extension activation, and via the project-local `.claude/mcp.json` written during project creation.

---

## Files

| File | Purpose |
|------|---------|
| `aiSetupVerifier.ts` | File-presence checks + `gatherInventory` orchestrator; returns `AiVerificationResult` |
| `skillInspector.ts` | Walks `.claude/skills/`, parses YAML frontmatter, classifies skills (`demo-builder` / `adobe` / `unknown`) and records which Adobe `bundle` a nested skill came from |
| `mcpInspector.ts` | Inspects each `.claude/mcp.json` server (third-party via `@modelcontextprotocol/sdk` stdio client; the in-extension `demo-builder` server via a direct socket probe), returns tool inventory with TTL cache |
| `sessionMcpDetector.ts` | Reads `~/.claude.json` + `~/.claude/mcp-needs-auth-cache.json` to enumerate session-level Adobe MCPs |
| `index.ts` | Public API exports |

---

## Key APIs

### `verifyAiSetup(projectPath, extensionDistPath): Promise<AiVerificationResult>`

Runs four file-presence checks in parallel with `gatherInventory()`:

| Check | What it looks for |
|-------|-------------------|
| `claudeMd` | `.claude/CLAUDE.md` exists and is non-empty |
| `mcpConfig` | `.claude/mcp.json` exists and is valid JSON with a `mcpServers` key |
| `mcpBinary` | `{extensionDistPath}/mcp-server.js` exists |
| `skillFiles` | At least one `.md` file exists in `.claude/skills/` |

Returns `AiVerificationResult` with `{ status, checks, inventory }`. The aggregate `status` reflects the checks (`error > warning > ok`); the `inventory` field carries the three inspector outputs (see below).

### `gatherInventory(projectPath): Promise<AiInventory>`

Runs the three inspectors via `Promise.allSettled` so a failing inspector degrades to an empty list rather than failing the whole verification. Each failed inspector also populates a matching `*Error` field (e.g., `skillsError`, `mcpsError`, `sessionMcpsError`) so UIs can distinguish "no items" from "introspection failed."

### `inspectSkills(projectPath): Promise<SkillInventoryEntry[]>`

Walks `<project>/.claude/skills/` and returns one entry per `.md` file with `{ name, description, path, source, bundle? }`. Classification: a top-level filename in `DEMO_BUILDER_SKILL_FILES` → `'demo-builder'`; nested in a bundle subdir → `'adobe'`, with `bundle` set to that directory's `<prefix>` (`aem`, `appbuilder`); other top-level → `'unknown'`.

`DEMO_BUILDER_SKILL_FILES` lives in `@/types/ai` and is the **single home** for those filenames — `skillsWriter` builds its write list from the same constant. They were separate lists once and drifted: `diagnose-demo.md` shipped in the writer without being added here, so the AI Capabilities modal filed a first-party skill under "Custom". `bundle` exists for the same class of reason — `source: 'adobe'` only means "arrived in a bundle", so the UI grouped every bundle under one hardcoded "Adobe AEM" heading and labelled App Builder skills as AEM.

### `inspectAllServers(projectPath): Promise<McpInventoryEntry[]>` + `clearMcpCache(serverId?)`

Reads `<project>/.claude/mcp.json` and returns per-server `{ id, status, tools?, error? }`. Third-party servers are spawned via the SDK's `Client` + `StdioClientTransport`, paginating through `tools/list`; the in-extension `demo-builder` server (the entry whose `env` carries `DEMO_BUILDER_MCP_SOCKET`) is inspected via a direct Unix-socket probe instead — no subprocess, no SDK handshake — which avoids the spawn-and-loopback that starved past the 15s budget on first verify (the direct probe returns tool names only, with empty descriptions; the inventory UI shows the tool count). Module-level `Map` cache with 5-minute TTL (±10% jitter via `getCacheTTLWithJitter`). Per-server 15s overall budget (`MCP_INSPECT_TIMEOUT_MS`). Spawned children (the third-party path) inherit only the SDK's safe env allowlist (PATH/HOME/USER/SHELL/TERM/LANG/TMPDIR) — extension host secrets do not leak. `clearMcpCache()` clears every entry; `clearMcpCache(id)` clears a single entry.

### `detectSessionMcps(): Promise<SessionMcpEntry[]>`

Returns one entry per name in `~/.claude.json::claudeAiMcpEverConnected`, with `needsAuth` derived by cross-referencing `~/.claude/mcp-needs-auth-cache.json`. **Both files are undocumented Claude Code internal state**; `needsAuth: false` means "not currently flagged for re-auth" — not a hard "authenticated" guarantee.

---

## Architecture

The AI context files live in the project directory, not in this feature:

```
<projectPath>/
├── AGENTS.md              ← generated by aiContextWriter.ts (universal AI context)
├── CLAUDE.md              ← one-line pointer to AGENTS.md
├── .claude/
│   ├── CLAUDE.md          ← one-line pointer to AGENTS.md
│   ├── mcp.json           ← generated by mcpConfigWriter.ts
│   ├── settings.json      ← generated by claudeSettingsWriter.ts (PostToolUse
│   │                        git-sync hook + PreToolUse aio-global guard)
│   └── skills/                     ← generated by skillsWriter.ts
│       ├── add-component.md            (lifecycle ×4, incl. create-eds-project.md)
│       ├── sync-changes.md
│       ├── update-credentials.md
│       ├── create-eds-project.md
│       ├── diagnose-demo.md
│       ├── scrape-reference-site.md    (Playwright-gated ×3: written only when
│       ├── connect-authenticated-site.md   @playwright/mcp is installed —
│       ├── refine-visual-match.md          SKILL_MCP_TOOL_DEPENDENCIES)
│       ├── commerce-block-mapper.md
│       ├── demo-data-injector.md
│       ├── header-nav-footer.md
│       ├── register-custom-block.md
│       └── remove-custom-block.md
└── .mcp.json              ← generated by mcpConfigWriter.ts (Claude Code project scope)
```

The three writers live in `src/features/project-creation/services/`:
- `aiContextWriter.ts` — generates `AGENTS.md` and the `CLAUDE.md` pointers; the section builders (and the Wayfinder commit pin) live in its sibling `agentsMdSections.ts`. The "Finding Adobe Documentation" section points agents at [Wayfinder](https://github.com/adobe-commerce/wayfinder), Adobe's own routing guide across aem.live / storefront / Experience League / da.live / developer.adobe.com, so the bundle does not re-derive that map. Pinned to a reviewed commit rather than `@main` — a remote document that becomes agent instructions must not change without our review.
- `skillsWriter.ts` — writes `.claude/skills/*.md` guides
- `mcpConfigWriter.ts` — generates `.mcp.json`, `.claude/mcp.json`, `.claude/settings.json`

The orchestrator is `aiBundleService.generateAIContextFiles()` (tier functions `refreshMcpConfigs` / `refreshContextAndSkills` beside it; every file goes through the `generatedFileWriter` hash-and-skip seam per ADR-013), called as Phase 6 of project creation and from `aiHandlers.ts` for on-demand regeneration.

The **MCP server** runs *in-extension* (`server/inExtensionMcpServer.ts`) on a per-workspace Unix socket, reached through the `dist/mcp-proxy.js` stdio↔socket forwarder Claude Code spawns. It exposes the full agent tool surface (auth, project lifecycle, cloud resources, storefront, updates) — see `docs/systems/mcp-server.md` for the complete catalog. The eight **file-based project tools** below are the `vscode`-free subset, registered by `registerProjectTools` in `src/mcp-server.ts` (a shared module; its old standalone process is retired):

| Tool | Description |
|------|-------------|
| `list_projects` | Lists all Demo Builder projects (supports `offset`/`limit`) |
| `get_project` | Reads `.demo-builder.json` — token-lean summary by default; `full=true` for the complete manifest |
| `get_component_config` | Reads `.demo-builder.json` or a `.env` file |
| `update_project_config` | Writes `.demo-builder.json` or a `.env` file |
| `sync_storefront` | Git add / commit / push in the storefront directory |
| `list_blocks` | Lists block directory names (supports `offset`/`limit`) |
| `promote_block_to_library` | Registers a custom block in DA.live's authoring picker (updates `component-definition.json`, writes the doc page, appends the sheet row, commits + pushes, publishes). `unsafeHTML` is sanitized via `sanitize-html` at the MCP boundary — see `sanitizeBlockHtml()` in `src/mcp-server.ts` for the allowlist. Optional `description` (≤1,000 chars) is persisted to `component-definition.json::components[].description` and rendered as a picker-tile tooltip by the EDS authoring runtime. |

Responses are shaped to keep token usage low: tool output is emitted as compact JSON (no pretty-print indentation), `get_project` collapses large arrays/metadata unless `full=true`, and `get_block_source` is progressive (manifest first, one file per fetch, with a per-file byte cap) rather than dumping every file at once.

---

## Token Efficiency

The MCP server is convenient but every tool call spends the agent's context-window tokens. There are two independent levers — one we own in this repo, one that lives in the harness (the Claude Code CLI).

### Lever 1 — Response shaping (server-side, always in effect)

This is the portable win: it helps regardless of which CLI version a user runs. The dominant cost is tool **response payloads**, not the tool definitions, so the server keeps responses lean:

- **Compact JSON.** `get_project` emits `JSON.stringify(x)` (no indentation). Pretty-print whitespace is pure token waste for machine-consumed output.
- **Summaries over full dumps.** `get_project` returns a curated summary by default — `aiPrompts` collapses to a count, `installedBlockLibraries` keeps name/source but replaces `blockIds` with a count, and `componentInstances` keeps `path` while dropping metadata blobs. Pass `full=true` for the untouched manifest.
- **Pagination.** `list_projects` and `list_blocks` accept `offset`/`limit`.

When adding or changing a tool, keep this in mind: prefer a list-then-fetch shape over bulk dumps, return compact JSON, and cap any file/blob a tool can return.

### Lever 2 — Deferred tool loading (harness-side, version-dependent)

Recent Claude Code versions support **tool-search / deferred tool loading**: a server's tool *definitions* are not all injected into the model's context up front — the harness surfaces tool names and pulls a tool's full schema only when it's about to be used. This makes the static cost of the tool list pay-per-use instead of per-turn.

There is nothing to enable in `mcp-server.ts` for this — it is governed by the user's CLI version. To take advantage of it:

- **Encourage a current Claude Code CLI.** Older CLIs eager-load every registered server's full tool list into context on every turn; deferred loading only kicks in on recent versions.
- **Keep tools search-friendly.** Concise, distinct tool names and one-line descriptions (which we already use) let the harness match the right tool on demand. Don't bury intent in long prose — search ranks on names and short descriptions.

### How the two levers combine with project-scoping

Global registration (`~/.claude.json`) is a manual opt-in via the **Demo Builder: Register Global MCP** command — its entry carries no pinned socket, so the proxy discovers a running extension window at launch (`server/mcpSocketDiscovery.ts`; enables `create_project`/`list_projects` from an arbitrary cwd). The default is per-project `.mcp.json`. The net effect:

- **Project scope** keeps the server out of unrelated Claude sessions entirely (the tools load only in a demo-project directory).
- **Deferred loading** trims the in-session static cost where the server *is* active.
- **Response shaping** trims the dynamic per-call cost — and is the only lever that helps on CLIs without deferred loading.

---

## Integration Points

- **Standalone AI surface** (`src/features/dashboard/ui/aiSurface/AiOverviewScreen.tsx`): renders a status strip, capability bullets, an installed-skills drill-down, and a sidebar of curated prompt cards plus an Open-in-Claude-Code CTA. Actions: Regenerate AI Files (`regenerate-ai-files` then re-verify). There is no Refresh action — the `inspect-mcp` handler it once called was removed 2026-08-05 as unreachable.
- **`verify-ai-setup` handler** (`aiHandlers.ts`): returns `AiVerificationResult` so the AI dashboard can render the AI-readiness badge from a single round-trip.
- **`Open in Claude Code` command** (`src/commands/openInClaude.ts`): launches the Claude Code CLI in a VS Code integrated terminal placed as a tab in the active editor group (next to Project Dashboard). Spawn: `claude --continue -- '<prompt>'` (race-free — claude receives the prompt as a launch argument and auto-submits). Reuse: when a live "Claude Code" terminal exists, the existing session is focused and the prompt is injected via bracketed paste (CSI 200~ / 201~), which pre-fills the input for the user to send. The clipboard is always written too as a silent fallback (with a once-ever tip toast). The earlier "extension" surface — URI-launching the Claude Code VS Code extension — was retired: the extension's URI handler opens a new chat on every launch (no public API to inject a prompt into the live chat), so the wand's "pick a prompt, drop it into the conversation" model can't work there. Dispatched from the dashboard tile, the project-card kebab menu, the wand QuickPick prompt rows (with workspace anchor + pending-prompt mechanism), and the activation handler that replays pending launches.
- **`copyAiPrompt` handler** (`aiHandlers.ts`): writes a prompt body to the clipboard via `vscode.env.clipboard.writeText` and shows a confirmation toast. Used by the kebab "Copy prompt" action on each prompt card. Logs the prompt name only — never the body.
- **Project creation** (`aiBundleService.ts`, invoked from the creation executor): `generateAIContextFiles()` runs all three writers after project setup completes. The activation sweep (`aiBundleActivationRefresh.ts`) additionally repairs config paths silently on every start and refreshes content when a project's `aiContextVersion` stamp is stale (ADR-013 hash-and-skip; user-edited files are kept and reported). Also runs as the post-step of `Demo Builder: Check for Updates` when an Adobe MCP package update is applied. The per-project `.mcp.json` written during project creation exposes the tools in-project and only loads them where relevant; global (`~/.claude.json`) registration is never automatic — it exists only as the explicit **Demo Builder: Register Global MCP** opt-in (see above).

---

## Testing

Tests for this feature live in `tests/features/ai/`:
- `aiSetupVerifier.test.ts` — file-presence checks + inventory payload + `gatherInventory` orchestration
- `skillInspector.test.ts` — classification + frontmatter parsing
- `mcpInspector.test.ts` — SDK client integration, pagination, timeouts, cache lifecycle, env allowlist
- `sessionMcpDetector.test.ts` — `~/.claude.json` + needs-auth cache cross-reference
- `mcpServer.test.ts` — standalone MCP server tools

Tests for the three project-creation services live in `tests/features/project-creation/services/`.
