# The order items run in, one name per line. Blank lines and # comments ignored.
#
# Run 2 (2026-09-03): PL-32 is finished and out. Only PL-16 remains, with a
# condition that means exhaustion rather than movement — the first run's
# condition ("the ceiling is lower than it starts") was satisfied by batch one
# and ended the goal with 134 sites still unread.

PL-16
