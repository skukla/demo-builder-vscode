{
 "_what": "Skills the AI Capabilities modal promises for the measured shapes that NO battery prompt has yet proven (the skills-side sibling of unprompted-baseline.json). An entry leaves when a prompt with expectSkill scores skillHit against it. Like the tool baseline, this may only SHRINK.",
 "_gate": "Measurement is only meaningful on a bundle carrying AI_CONTEXT_VERSION >= 27: the flat pre-v27 layout is never registered as a skill (measured live 2026-08-27 \u2014 every directory-format skill registered, zero flat files). SATISFIED since 2026-08-27 evening: both bundles verified v27 on disk (bodea manifest aiContextVersion=27; home + project skills are <name>/SKILL.md directories). Entries leave ONLY on a measured skillHit from the skill-*-home / skill-*-project prompt set \u2014 never on layout verification alone.",
 "_ratchet": "No automatic ratchet test enforces this file yet (the tool baseline has toolPromptCoverage.test.ts; the skills one is added with the first skill batch). Saying so here rather than silently: a cap nobody enforces reads as covered when it is not.",
 "_shapes": "home = the projects root (cwd default); project = inside bodea (prompts declare cwd: 'project'). The demo-builder set is promised on both; the aem bundle only inside an EDS project. bodea builds no App Builder app, so the appbuilder bundle is correctly absent there (AI-1o).",
 "home": [],
 "project": [],
 "_measured": "2026-08-28 (runs 00-30-00Z home-calibration through the two scope batches): 34/34 skillHit \u2014 3 calibration + 14 home + 17 project, every prompt's Skill invocation matched its expectation. Both writers verified: homeAiContextWriter (home scope) and skillsWriter (project scope), on v27 directory-layout bundles. The item's open question is answered: headless `claude -p` runs DO register and invoke project skills identically to interactive sessions."
}
