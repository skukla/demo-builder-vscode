# Dream runs

Out-of-band curation passes over memory, skills, and CLAUDE.md (the `dream` skill).
One file per run: `<YYYY-MM-DD>.md`, holding accepted AND rejected proposals — the
rejection record is what stops a later run re-litigating settled items.

Last dream run: 2026-08-24 (at the v1.0.0-beta.142 cut).
`.rptc/CLAUDE.md` documents the BROKEN jest redirect order (`2>&1 > file`) in two places, which is
why 4 of 6 sessions used it; proposed fixing the docs, extending the jest hook to block it, and
settling the `/tmp`-vs-scratchpad contradiction that 6 of 6 sessions resolve against the guidance.

Previous: 2026-08-18 (at the v1.0.0-beta.133 cut).
`spectrum-webview-ui` §Verify rewritten around "you cannot see this UI" after 16 screenshot-bearing
user turns in 3/4 sessions; 9 shipped plans moved out of `.rptc/plans/`; quote-your-globs; the
`project_appbuilder_app_family` memory pruned ~45 lines -> ~30 after every branch-state claim in it
went false; and — proposed from the run's OWN mistakes — the control-scope rule, after five wrong
answers in one session from correct commands aimed at the wrong tree, one of which nearly shipped a
wrong setting key in the release notes. Two prior open items CLOSED with measurements: the
2026-08-07 verifying rule stuck (positive controls 0.2–0.9% -> 3.0–6.2% per 100 Bash calls) and the
skill-invocation gap opened 2026-07-31 is gone (5.3/7.4/15.1% vs 2.1/4.0% baselines).
**Next run: check whether the control-scope rule took** — the measurable signal is wrong-tree
negatives, not control COUNT, which the 2026-08-07 rule already drove up.)

Prior: 2026-08-07 (2 sessions / 26,034 turns — 3 proposals, all accepted: exit-codes consumed by
pipes, tests that cannot fail, backlog-vs-plans duplication)

Last dream run: 2026-08-18 (at the v1.0.0-beta.133 cut) — 4 sessions; 2 proposals
(dead branch references in memory; the AI_CONTEXT_VERSION pin test), both pending.
