# Dream run — 2026-08-18

Sessions reviewed: 4 (2026-08-17 → 2026-08-18) · Corpus: 27 memories, 30 project skills
Run at the `v1.0.0-beta.133` cut. Propose-only — nothing below is applied.

Prior runs: 2026-08-11b, -15, -16, -17. The corpus is in good shape and most signals
this pass were already-closed loops; the two findings below are what survived.

---

## Proposed 1 — [WRONG, highest severity] Seven memories send a future session to branches that no longer exist

**Finding.** Memory names seven feature branches; **four have been deleted** and the repo
now holds only `develop`, `master`, `docs/mcp-surface-for-sc-design` and
`claude/commerce-connect-slice-1-plan-bgVlb`. Worse than dead links: three of the
references assert that SHIPPED work is unmerged, which instructs the next session to
distrust code that is on `develop`.

**Evidence.**
- `project_backlog_directory.md:20` — *"The **App Builder attach feature (Model A)** is
  live work on **unmerged branches** (`feature/appbuilder-app-deploy-spine` = slice 1/5
  `20fae62f`; `feature/appbuilder-app-curated-catalog` = slice 2). Slices 3–5 … exist
  **only on those branches, not develop** — don't cite them as canonical here until
  merged."*
  Verified false: `git merge-base --is-ancestor 20fae62f develop` → **on develop**, and
  `src/features/project-creation/config/app-builder-components.json` (the slice-2
  catalog) exists on develop. Both branches are gone.
- `project_store_discovery_feature.md:11` — *"Next steps: deploy `accs-discovery-service`
  …, then open a PR from `feature/store-discovery` → `develop`."*
  That branch does not exist, and `src/features/eds/services/commerceStoreDiscovery.ts`
  is on develop. The instruction cannot be followed and its premise is wrong.
- `project_bodea_template.md:90` — still reads *"work is committed and pushed on
  `feature/bodea-template`"*. Line 13 of the same file was corrected earlier today when
  the branch was deleted; **line 90 was missed** — a one-site fix on a multi-site claim.
- `project_backlog_directory.md:33` (`feature/chat-first-ai`) and
  `project_builder_nested_design.md:12,34,35`
  (`feature/project-builder-ux`, `feature/adobe-io-project-creation`,
  `feature/adobe-io-workspace-creation`) — same shape. Note `4210339d` and `637f9a1a`
  do **not** resolve as ancestors of develop; whether that work shipped under rebased
  hashes is **unverified** and should be checked before those lines are rewritten
  rather than assumed either way.

Prevalence: 6 memory files, 1 session to surface (this one). Structural, not statistical
— a deleted branch never announces itself, so nothing else would ever catch these.

**Change.** For each site: replace the branch reference with the verified location
(`develop`, with the commit that carries it) or, where unverified, an explicit
*"branch deleted; whether this shipped is unchecked — verify the symbol on develop
before acting."* Do **not** silently delete the lines; the fact that work moved is
itself the useful record. Add to `project_backlog_directory.md` the general rule that a
branch name in memory is a perishable claim.

**Risk if wrong.** Low. The edit only ever narrows a claim to what was measured. The one
real risk is asserting "shipped" for `4210339d`/`637f9a1a` without checking — which is
why the change above forbids exactly that.

---

## Proposed 2 — [MISSING] `ai-context-authoring` omits the test that pins the constant it is entirely about

**Finding.** Discipline 1 of the skill is "hand-bump `AI_CONTEXT_VERSION`". Its
**Test pins that move** section lists four suites — `skillsWriter.test.ts`,
`aiContextWriter.writeAgentsMd.test.ts`, `aiHandlers-*.test.ts`, `mcpConfigWriter.test.ts`
— and does **not** name `tests/core/constants.test.ts`, which is the only test that
asserts the version value.

**Evidence.** This session: the bump to 15 landed in `34793643`; the pin still said 14
and the failure surfaced only on the full suite at release pre-flight, costing a
separate follow-up commit (`74f60228`). The scoped gate ran `tests/features/…` and never
reached `tests/core/`. Verified by reading the skill:
`grep -n "constants.test" .claude/skills/ai-context-authoring/SKILL.md` → no match.

Prevalence: 1 session for the miss (prior bumps updated the pin in the same commit).
Proposed anyway because the gap is **structural rather than statistical** — the section
exists precisely so nobody has to hunt for pins, and it omits the most direct one.

**Change.** Add to that section:

> - `tests/core/constants.test.ts` — asserts the literal `AI_CONTEXT_VERSION` value and
>   carries the per-version rationale comment. It lives OUTSIDE `tests/features/`, so a
>   feature-scoped jest run will not catch it; bump the pin in the same commit as the
>   constant.

**Risk if wrong.** None meaningful — it names a file that exists and a real coupling.

---

## Notes (single occurrence — not yet actionable)

- **Scoped-gate blind spot.** The same miss suggests `gate` could say that a change to
  `src/core/constants.ts` implies `tests/core/`. Not proposed: the skill already carries
  the general rule ("When production code changed, locate and run its complementary
  tests"), and Proposed 2 fixes the specific coupling at the place someone is actually
  reading. Re-raise only if a second `tests/core/` miss appears.

## Rejected / already closed (not re-proposing)

- **`appbuilder-api-subscription` reuse instruction buried at the end.** Raised in the
  2026-08-17 session — *"the guidance was in a skill I had just loaded … I read the skill
  top-down, started writing against the raw SDK, and never reached the last paragraph."*
  **Already fixed**: the instruction is now a top section, *"Before writing any SDK call:
  this repo already wraps these"*, at line 17 of 60. No action.
- **"A named field in an API response is a LEAD, not a finding."** The other failure from
  that session. Already folded into root `CLAUDE.md` by a prior run, with the
  `ACCS-REST-API … enabled: false` example. No action.
- **Hook blocks / recurring tool-error shapes.** Mined across the four sessions: zero real
  hook blocks (68 apparent matches were the substring "block" in ordinary stdout — a
  false positive in the mining regex, not a signal) and no recurring error shape. Nothing
  to propose.
